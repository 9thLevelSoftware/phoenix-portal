---
phase: 08-tech-debt-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/Navigation.tsx
  - src/app/components/MobileBottomNav.tsx
  - src/app/components/CycleBuilder.tsx
  - src/app/components/CycleBuilderMain.tsx
  - src/app/components/Dashboard.tsx
autonomous: true

must_haves:
  truths:
    - "Biomechanics appears in the desktop Navigation sidebar between Analytics and Challenges"
    - "Biomechanics appears in the MobileBottomNav More drawer"
    - "CycleBuilder routine picker shows real user routines from Supabase (not hardcoded mock data)"
    - "Dashboard Active Challenges section shows an empty placeholder state instead of mock challenge data"
    - "Dashboard Recent Badges section shows an empty placeholder state instead of mock badge data"
  artifacts:
    - path: "src/app/components/Navigation.tsx"
      provides: "Biomechanics entry in navItems array"
      contains: "biomechanics"
    - path: "src/app/components/MobileBottomNav.tsx"
      provides: "Biomechanics entry in moreItems array"
      contains: "biomechanics"
    - path: "src/app/components/CycleBuilder.tsx"
      provides: "Real routine fetching via routineListOptions"
      contains: "routineListOptions"
    - path: "src/app/components/Dashboard.tsx"
      provides: "Empty states for challenges and badges"
      contains: "No active challenges"
  key_links:
    - from: "src/app/components/CycleBuilder.tsx"
      to: "src/queries/routines.ts"
      via: "useQuery(routineListOptions(user.id))"
      pattern: "routineListOptions"
    - from: "src/app/components/Navigation.tsx"
      to: "/biomechanics route"
      via: "navItems path entry"
      pattern: "path:.*biomechanics"
---

<objective>
Add Biomechanics to navigation menus, replace CycleBuilder mock routines with Supabase queries, and replace Dashboard mock challenges/badges with empty states.

Purpose: Closes 3 of 5 tech debt items from the v1 audit -- navigation discoverability (BIO-09), last remaining mock data arrays in CycleBuilder and Dashboard.
Output: Navigation shows Biomechanics, CycleBuilder loads real routines, Dashboard shows clean empty states for challenges/badges.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@src/app/components/Navigation.tsx
@src/app/components/MobileBottomNav.tsx
@src/app/components/CycleBuilder.tsx
@src/app/components/CycleBuilderMain.tsx
@src/app/components/Dashboard.tsx
@src/app/components/cycle-builder/types.ts
@src/queries/routines.ts
@src/schemas/transforms.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Biomechanics to navigation and replace Dashboard mock data with empty states</name>
  <files>
    src/app/components/Navigation.tsx
    src/app/components/MobileBottomNav.tsx
    src/app/components/Dashboard.tsx
  </files>
  <action>
**Navigation.tsx:**
1. Add `Activity` to the lucide-react import (may already be available, check first -- if not, import it).
2. Add `{ path: '/biomechanics', label: 'Biomechanics', icon: Activity }` to the `navItems` array (line 25-36) between Analytics and Challenges entries. This positions it next to its analytics-adjacent peers.

**MobileBottomNav.tsx:**
1. Add `Activity` to the lucide-react import.
2. Add `{ path: '/biomechanics', label: 'Biomechanics', icon: Activity }` to the `moreItems` array (line 34-41). Position it after Records and before Challenges. Do NOT add to `primaryItems` -- keep the 4-item primary bar intact.

**Dashboard.tsx:**
1. Remove the `activeChallenges` mock array (lines 72-76) entirely.
2. Remove the `badges` mock array (lines 79-84) entirely.
3. Replace the Active Challenges rendering block (lines 393-425, the `<Card>` inside the motion.div) with an empty-state card:
   - Keep the existing Card and motion.div wrapper with the same class names
   - Show the heading "Active Challenges" with the same styling
   - Show a centered empty state: Trophy icon (w-8 h-8, text-[#374151]), "No active challenges yet" text (text-sm text-[#9CA3AF]), subtitle "Join challenges from the Challenges page to track your progress here" (text-xs text-[#6B7280])
   - Keep the Button at the bottom but make it navigate to `/challenges` using `useNavigate` or a Link component. Import `Link` from react-router and use `<Link to="/challenges">` wrapping the button, or use `<Button asChild>` with Link.
4. Replace the Badge Showcase rendering block (lines 427-454, the `<Card>` inside the motion.div) with an empty-state card:
   - Keep the existing Card and motion.div wrapper
   - Show heading "Recent Badges"
   - Show centered empty state: Award icon (w-8 h-8, text-[#374151]), "No badges earned yet" text (text-sm text-[#9CA3AF]), subtitle "Complete challenges and hit milestones to earn badges" (text-xs text-[#6B7280])
5. Remove the `Progress` import from ui/progress IF it's only used by the challenges section (check first). Also remove `Badge` import if only used by PRs section (it IS used for PR "NEW" badge, so keep it).
  </action>
  <verify>
1. `npm run build` succeeds without errors
2. Grep `navItems` in Navigation.tsx shows biomechanics entry
3. Grep `moreItems` in MobileBottomNav.tsx shows biomechanics entry
4. Grep `mockRoutines\|activeChallenges\|badges =` in Dashboard.tsx returns no matches (mock arrays removed)
  </verify>
  <done>
Biomechanics is in both desktop navItems and mobile moreItems arrays. Dashboard shows empty-state UI for challenges and badges instead of hardcoded mock data.
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace CycleBuilder mock routines with Supabase query</name>
  <files>
    src/app/components/CycleBuilder.tsx
    src/app/components/CycleBuilderMain.tsx
    src/app/components/cycle-builder/types.ts
  </files>
  <action>
**CycleBuilder.tsx** (the active routed version):
1. Add imports:
   - `import { useQuery } from '@tanstack/react-query';`
   - `import { useAuth } from '@/app/hooks/useAuth';`
   - `import { routineListOptions } from '@/queries/routines';`
   - `import type { Routine as PickerRoutine } from './cycle-builder/types';`
2. Inside the `CycleBuilder` component function, add after existing state declarations:
   ```typescript
   const { user } = useAuth();
   const { data: routinesRaw } = useQuery({
     ...routineListOptions(user?.id ?? ''),
     enabled: !!user?.id,
   });
   ```
3. Create the mapping from Supabase routines to the CycleBuilder's inline routine shape. Note: CycleBuilder.tsx uses an inline type `{ id: string; name: string; exercises: number; duration: number; muscleGroup: string }` (not the `Routine` from types.ts which also has `lastUsed`). Map accordingly:
   ```typescript
   const routines = (routinesRaw ?? []).map((r) => ({
     id: r.id,
     name: r.name,
     exercises: r.exercise_count,
     duration: r.estimated_duration,
     muscleGroup: r.tags?.[0] ?? 'General',
   }));
   ```
4. Delete the `mockRoutines` array (lines 68-74).
5. Find ALL references to `mockRoutines` in the file and replace with `routines`. The RoutinePickerModal section around line 116+ uses `mockRoutines` -- update to `routines`.
6. If routines are loading (no data yet), the RoutinePickerModal should show an empty state or loading message instead of nothing.

**CycleBuilderMain.tsx** (orphan file -- not imported anywhere but still needs cleanup):
1. Apply the same pattern: add imports for useQuery, useAuth, routineListOptions.
2. Since this component takes props (not using useParams), check if `useAuth()` is appropriate here. It should be fine since this component would only render in an authenticated context.
3. Add the same query and mapping, using the `Routine` type from `./cycle-builder/types.ts` which has `lastUsed`:
   ```typescript
   const routines: Routine[] = (routinesRaw ?? []).map((r) => ({
     id: r.id,
     name: r.name,
     exercises: r.exercise_count,
     duration: r.estimated_duration,
     muscleGroup: r.tags?.[0] ?? 'General',
     lastUsed: r.last_used_at ? new Date(r.last_used_at) : undefined,
   }));
   ```
4. Delete the `mockRoutines` array (lines 52-58).
5. Replace all `mockRoutines` references with `routines`.

**PITFALL: The `routines` Supabase table has NO `muscle_group` column.** Use `tags[0]` as the mapping target. The `tags` field is `string[] | null` in the Zod schema.

**PITFALL: last_used_at is a Date from Zod transform, not a string.** The CycleBuilder types.ts Routine expects `lastUsed?: Date`. Pass `r.last_used_at ?? undefined` (it's already a Date | null from Zod).
  </action>
  <verify>
1. `npm run build` succeeds without TypeScript errors
2. Grep `mockRoutines` across `src/app/components/CycleBuilder*.tsx` returns zero matches
3. Grep `routineListOptions` in CycleBuilder.tsx confirms the real query is wired in
  </verify>
  <done>
Both CycleBuilder files use routineListOptions from Supabase instead of hardcoded mockRoutines. Type mapping correctly handles exercise_count->exercises, estimated_duration->duration, tags[0]->muscleGroup conversions.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. No references to `mockRoutines`, `activeChallenges` mock array, or `badges` mock array remain in the modified files
3. Navigation.tsx navItems contains `/biomechanics` entry
4. MobileBottomNav.tsx moreItems contains `/biomechanics` entry
5. CycleBuilder.tsx imports and uses `routineListOptions`
6. Dashboard.tsx shows empty-state UI (grep for "No active challenges" and "No badges earned")
</verification>

<success_criteria>
- Biomechanics discoverable in both desktop sidebar and mobile More drawer
- CycleBuilder routine picker fetches real data from Supabase
- Dashboard challenges and badges sections show clean empty/placeholder UI
- Build passes with no new TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-tech-debt-cleanup/08-01-SUMMARY.md`
</output>
