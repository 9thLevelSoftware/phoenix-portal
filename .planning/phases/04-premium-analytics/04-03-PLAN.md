---
phase: 04-premium-analytics
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/components/charts/VelocityProfile.tsx
  - src/app/components/charts/PowerOutput.tsx
autonomous: true

must_haves:
  truths:
    - "Per-rep velocity bars show mean and peak velocity with VBT zone color coding"
    - "Zone classification labels (Strength/Speed/Power) appear per set or per rep"
    - "Power output displays watts per rep in a bar chart"
    - "Charts handle empty data gracefully (empty state, not crash)"
  artifacts:
    - path: "src/app/components/charts/VelocityProfile.tsx"
      provides: "Per-rep velocity bar chart with VBT zone colors and labels"
      exports: ["VelocityProfile"]
    - path: "src/app/components/charts/PowerOutput.tsx"
      provides: "Per-rep power output bar chart in watts"
      exports: ["PowerOutput"]
  key_links:
    - from: "src/app/components/charts/VelocityProfile.tsx"
      to: "src/lib/vbt.ts"
      via: "classifyVbtZone import for per-rep zone coloring"
      pattern: "import.*classifyVbtZone.*from.*@/lib/vbt"
    - from: "src/app/components/charts/PowerOutput.tsx"
      to: "src/lib/biomechanics.ts"
      via: "calculatePower import"
      pattern: "import.*calculatePower.*from.*@/lib/biomechanics"
---

<objective>
Build VBT velocity profile and power output chart components. These show per-rep mean/peak velocity with sport-science zone classification and force*velocity power in watts.

Purpose: BIO-05, BIO-06, BIO-07 -- velocity-based training insights and power tracking that transform raw sensor data into actionable training feedback.
Output: VelocityProfile and PowerOutput chart components
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-premium-analytics/04-RESEARCH.md
@.planning/phases/04-premium-analytics/04-01-SUMMARY.md
@src/lib/vbt.ts
@src/lib/biomechanics.ts
@src/app/components/charts/shared/ChartTheme.ts
@src/app/components/charts/shared/ChartTooltip.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build VelocityProfile chart with VBT zone color coding</name>
  <files>src/app/components/charts/VelocityProfile.tsx</files>
  <action>
    Create `src/app/components/charts/VelocityProfile.tsx`:

    **Props interface `VelocityProfileProps`:**
    - `repSummaries: RepSummary[]` -- array of rep summary data (from queries/telemetry repSummariesOptions)
    - `height?: number` -- default 280
    - `showPeakVelocity?: boolean` -- default true, shows peak velocity as lighter overlay bar
    - `showZoneLabels?: boolean` -- default true

    **Implementation:**
    1. Wrap in `ParentSize` for responsive width
    2. Use visx `<Bar>` from `@visx/shape` for vertical bar chart
    3. X-axis: rep number (1, 2, 3...) using `scaleBand` from `@visx/scale`
    4. Y-axis: velocity in m/s using `scaleLinear`
    5. For each rep:
       - Call `classifyVbtZone(rep.mean_velocity_mps)` to get zone info
       - Render primary bar with zone's color for mean velocity
       - If `showPeakVelocity`, render a lighter (opacity 0.4) bar behind for peak velocity
       - If `showZoneLabels`, render zone label text above bar group
    6. Add a zone legend below the chart showing all 5 zones with color swatches
    7. Handle empty repSummaries: render a centered "No velocity data available" message styled with `text-gray-500`
    8. Axes: AxisBottom with rep numbers, AxisLeft with "Velocity (m/s)" label, both using CHART_COLORS
    9. Tooltip on bar hover: show rep number, mean velocity, peak velocity, zone name, description
    10. Export as named export
  </action>
  <verify>
    `npm run build` succeeds. VelocityProfile exports correctly. Imports from @visx/shape and @/lib/vbt resolve.
  </verify>
  <done>
    VelocityProfile renders per-rep velocity bars color-coded by VBT zone with optional peak velocity overlay and zone legend. Empty state handled gracefully.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build PowerOutput bar chart component</name>
  <files>src/app/components/charts/PowerOutput.tsx</files>
  <action>
    Create `src/app/components/charts/PowerOutput.tsx`:

    **Props interface `PowerOutputProps`:**
    - `repSummaries: RepSummary[]` -- same rep summary data
    - `height?: number` -- default 250
    - `highlightPeak?: boolean` -- default true, highlights the highest power rep

    **Implementation:**
    1. Wrap in `ParentSize`
    2. Compute power per rep: use `rep.power_watts` from the summary (already pre-computed server-side). If power_watts is null/zero, compute client-side with `calculatePower(rep.mean_force_n, rep.mean_velocity_mps)`.
    3. Bar chart with visx `<Bar>`:
       - X-axis: rep number (scaleBand)
       - Y-axis: power in watts (scaleLinear)
       - Bar color: Ember (#FF6B35) by default
       - If `highlightPeak`, the rep with highest watts gets Gold (#F59E0B) color, others get dimmed (opacity 0.6)
    4. Add a text label above each bar showing the watt value (e.g., "342W")
    5. Axes: AxisBottom with rep numbers, AxisLeft with "Power (W)" label
    6. Handle empty data: "No power data available" message
    7. Tooltip on hover: rep number, power watts, force, velocity used in calculation
    8. Export as named export
  </action>
  <verify>
    `npm run build` succeeds. PowerOutput exports correctly. Imports from @/lib/biomechanics resolve.
  </verify>
  <done>
    PowerOutput renders per-rep power bars in watts with peak rep highlighting and value labels. Graceful empty state. Build passes.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. VelocityProfile uses classifyVbtZone for zone coloring (key link)
3. PowerOutput uses calculatePower as fallback for missing power_watts (key link)
4. Both components handle empty data arrays without crashing
5. Both use Phoenix theme colors from ChartTheme
6. Zone legend renders all 5 VBT zones with correct colors
</verification>

<success_criteria>
- VelocityProfile shows distinct VBT zone colors per rep bar
- PowerOutput highlights peak power rep visually
- Both charts are responsive via ParentSize
- Empty states render informative messages instead of blank charts
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-premium-analytics/04-03-SUMMARY.md`
</output>
