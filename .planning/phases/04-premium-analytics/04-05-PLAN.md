---
phase: 04-premium-analytics
plan: 05
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04", "04-06"]
files_modified:
  - src/app/components/Biomechanics.tsx
  - src/app/components/MuscleHeatmap.tsx
  - src/app/components/ConsistencyCalendar.tsx
  - src/app/routes/index.tsx
autonomous: true

must_haves:
  truths:
    - "Biomechanics dashboard page is accessible at /biomechanics route and gated behind PHOENIX tier"
    - "FREE users see UpgradePrompt instead of biomechanics content"
    - "Dashboard shows force curves, velocity profiles, asymmetry, power output, and ROM sections"
    - "Muscle heatmap shows volume distribution across 6 body regions with color intensity"
    - "Consistency calendar shows workout days in a GitHub-style grid with streak tracking"
    - "Exercise progress section shows weight/volume/1RM trend charts for selected exercise"
    - "Summary report section shows weekly/monthly metrics cards with volume, frequency, PRs, and consistency"
  artifacts:
    - path: "src/app/components/Biomechanics.tsx"
      provides: "Premium biomechanics dashboard page integrating all chart components"
      exports: ["Biomechanics"]
    - path: "src/app/components/MuscleHeatmap.tsx"
      provides: "SVG body map with muscle group volume coloring"
      exports: ["MuscleHeatmap"]
    - path: "src/app/components/ConsistencyCalendar.tsx"
      provides: "GitHub-style workout frequency calendar heatmap"
      exports: ["ConsistencyCalendar"]
    - path: "src/app/routes/index.tsx"
      provides: "Updated route tree with /biomechanics route"
  key_links:
    - from: "src/app/components/Biomechanics.tsx"
      to: "src/app/components/SubscriptionGate.tsx"
      via: "SubscriptionGate wrapping entire page content"
      pattern: "<SubscriptionGate requiredTier=\"PHOENIX\">"
    - from: "src/app/components/Biomechanics.tsx"
      to: "src/app/components/charts/ForceCurve.tsx"
      via: "ForceCurve import for force curves section"
      pattern: "import.*ForceCurve.*from.*@/app/components/charts/ForceCurve"
    - from: "src/app/routes/index.tsx"
      to: "src/app/components/Biomechanics.tsx"
      via: "lazy import and Route path /biomechanics"
      pattern: "path=\"/biomechanics\""
    - from: "src/app/components/Biomechanics.tsx"
      to: "src/app/components/ExerciseProgress.tsx"
      via: "ExerciseProgress import for exercise progress section"
      pattern: "import.*ExerciseProgress.*from.*@/app/components/ExerciseProgress"
    - from: "src/app/components/Biomechanics.tsx"
      to: "src/app/components/SummaryReport.tsx"
      via: "SummaryReport import for summary section"
      pattern: "import.*SummaryReport.*from.*@/app/components/SummaryReport"
---

<objective>
Build the biomechanics dashboard page -- the premium analytics hub that integrates all chart components from plans 02-04, exercise progress and summary reports from plan 06, plus new muscle heatmap and consistency calendar components. Gate the entire page behind PHOENIX subscription tier.

Purpose: BIO-09, BIO-10, BIO-11, BIO-12, BIO-13 -- the page where premium subscribers get the "cannot get anywhere else" experience.
Output: Biomechanics page, MuscleHeatmap, ConsistencyCalendar, route registration (integrating ExerciseProgress + SummaryReport from 04-06)
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-premium-analytics/04-RESEARCH.md
@.planning/phases/04-premium-analytics/04-02-SUMMARY.md
@.planning/phases/04-premium-analytics/04-03-SUMMARY.md
@.planning/phases/04-premium-analytics/04-04-SUMMARY.md
@.planning/phases/04-premium-analytics/04-06-SUMMARY.md
@src/app/components/SubscriptionGate.tsx
@src/app/components/Analytics.tsx
@src/app/routes/index.tsx
@src/styles/theme.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MuscleHeatmap and ConsistencyCalendar components</name>
  <files>
    src/app/components/MuscleHeatmap.tsx
    src/app/components/ConsistencyCalendar.tsx
  </files>
  <action>
    1. Create `src/app/components/MuscleHeatmap.tsx`:
       - **Props:** `muscleVolumes: Record<string, number>` -- maps muscle group name to total volume in kg. Uses the 6-group taxonomy: Chest, Back, Legs, Shoulders, Arms, Core.
       - **Implementation:**
         - Custom SVG body outline (front view, simplified silhouette) -- NOT a third-party library. Keep it simple: ~150-200 lines of SVG path data for a stylized male body outline.
         - Define 6 SVG regions (path groups) corresponding to muscle groups:
           - Chest: pectoral area
           - Back: upper back/lats (shown from front as shoulders/width)
           - Legs: quadriceps + calves
           - Shoulders: deltoid area
           - Arms: biceps + triceps
           - Core: abdominal area
         - Color intensity: Map volume to opacity/color intensity. Min volume gets opacity 0.15, max gets 1.0. Use linear interpolation. Color base: Ember (#FF6B35).
         - On hover over a region: show tooltip with muscle group name and volume (e.g., "Chest: 2,450 kg")
         - Zero-volume regions: render with gray outline only (no fill)
         - Add labels next to each region (e.g., "Chest" text positioned near the pectoral paths)
       - **Mapping layer:** Create a `MUSCLE_GROUP_PATHS` constant mapping each group name to its SVG path IDs. Per research pitfall #5: map "Arms" to both bicep + tricep SVG regions.
       - Export as named export

    2. Create `src/app/components/ConsistencyCalendar.tsx`:
       - **Props:** `workoutDates: Date[]` -- array of dates the user worked out. `weeks?: number` -- default 52 (one year).
       - **Implementation:**
         - GitHub-style grid: 7 rows (Sun-Sat) x N columns (weeks)
         - Each cell is a small rect (10x10 with 2px gap) in an SVG
         - Color coding by workout count per day:
           - 0 workouts: #1A1A2E (dark background)
           - 1 workout: Ember at 40% opacity
           - 2 workouts: Ember at 70% opacity
           - 3+ workouts: Ember at 100% opacity
         - Day labels (M, W, F) on the left axis
         - Month labels (Jan, Feb...) along the top
         - Use `date-fns` (already installed) for date manipulation: `startOfWeek`, `eachDayOfInterval`, `format`, `isSameDay`
         - Calculate current streak and longest streak from workoutDates (consecutive days logic)
         - Display streak info below the calendar: "Current Streak: X days | Longest: Y days"
         - Tooltip on cell hover: date and workout count
       - Export as named export
  </action>
  <verify>
    `npm run build` succeeds. Both components export correctly. MuscleHeatmap renders SVG without errors. ConsistencyCalendar uses date-fns (already in package.json).
  </verify>
  <done>
    MuscleHeatmap renders a body outline with 6 muscle regions color-coded by volume. ConsistencyCalendar renders a GitHub-style year grid with workout frequency and streak tracking.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build Biomechanics dashboard page with SubscriptionGate and route registration</name>
  <files>
    src/app/components/Biomechanics.tsx
    src/app/routes/index.tsx
  </files>
  <action>
    1. Create `src/app/components/Biomechanics.tsx`:
       - Wrap entire page content in `<SubscriptionGate requiredTier="PHOENIX">` -- FREE users see UpgradePrompt.
       - Page layout following existing pattern (see Analytics.tsx for reference):
         - Page title "Biomechanics" with subtitle "Advanced training analytics"
         - Session/exercise selector at top: dropdown to pick a recent session, then an exercise within that session. Use shadcn `Select` component. Data from existing workouts query + session detail query.
         - Content sections in a responsive grid (1 col mobile, 2 col desktop):

         **Section 1: Force Curves** (spans full width)
         - `<ForceCurve>` component with set selector (tabs for Set 1, Set 2, etc.)
         - Toggle for "Overlay All Reps" vs "Single Rep" view
         - Toggle for "Normalized Time" vs "Actual Time"
         - Data: fetch rep_telemetry for selected set using `repTelemetryOptions`, group by rep_number, pass to ForceCurve

         **Section 2: Velocity & Power** (2 columns on desktop)
         - Left: `<VelocityProfile>` with rep summaries for selected set
         - Right: `<PowerOutput>` with same rep summaries

         **Section 3: Asymmetry** (full width)
         - `<AsymmetryGauge>` in per-rep mode for selected set
         - Summary badge showing session-level asymmetry average

         **Section 4: Range of Motion** (half width)
         - `<RomTrend>` for selected set

         **Section 5: Body Overview** (half width)
         - `<MuscleHeatmap>` with volume data from session's exercises

         **Section 6: Exercise Progress** (full width)
         - `<ExerciseProgress>` component (from plan 04-06) showing weight/volume/1RM trend charts
         - Pass userId and optionally pre-select the current session's exercise via initialExercise prop

         **Section 7: Summary Report** (full width)
         - `<SummaryReport>` component (from plan 04-06) showing weekly/monthly metrics cards
         - Pass userId prop

         **Section 8: Consistency** (full width)
         - `<ConsistencyCalendar>` with workout dates from workouts query

       - Use TanStack Query hooks: `useQuery(repTelemetryOptions(...))`, `useQuery(repSummariesOptions(...))`
       - Loading states: show Skeleton components while data loads
       - Error states: show error message card with retry button

    2. Update `src/app/routes/index.tsx`:
       - Add lazy import: `const Biomechanics = lazy(() => import('@/app/components/Biomechanics').then(m => ({ default: m.Biomechanics })));`
       - Add route inside ProtectedRoute > AppLayout: `<Route path="/biomechanics" element={<Biomechanics />} />`
       - Place after `/analytics` route for logical ordering
  </action>
  <verify>
    `npm run build` succeeds. Route `/biomechanics` exists in route tree. Biomechanics page imports SubscriptionGate with requiredTier="PHOENIX". All chart component imports resolve.
  </verify>
  <done>
    Biomechanics dashboard page exists at /biomechanics, gated behind PHOENIX tier. Page integrates ForceCurve, VelocityProfile, PowerOutput, AsymmetryGauge, RomTrend, MuscleHeatmap, ExerciseProgress, SummaryReport, and ConsistencyCalendar in a responsive grid layout. Session/exercise/set selectors allow navigation through workout data. Route registered and lazy-loaded.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `/biomechanics` route registered in routes/index.tsx with lazy loading
3. SubscriptionGate wraps page content with requiredTier="PHOENIX"
4. All 7 chart/analytics components (ForceCurve, VelocityProfile, PowerOutput, AsymmetryGauge, RomTrend, ExerciseProgress, SummaryReport) imported and rendered
5. MuscleHeatmap renders 6 muscle group regions with volume coloring
6. ConsistencyCalendar renders GitHub-style grid with streak tracking
7. Session/exercise/set selectors use existing query patterns
</verification>

<success_criteria>
- PHOENIX+ subscribers see the full biomechanics dashboard
- FREE users see UpgradePrompt (via SubscriptionGate default fallback)
- Page loads individual chart sections with skeleton loading states
- Telemetry fetched on-demand per selected set (not all at once)
- Responsive layout works on mobile (single column) and desktop (grid)
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-premium-analytics/04-05-SUMMARY.md`
</output>
