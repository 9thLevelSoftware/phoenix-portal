---
phase: 04-premium-analytics
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/components/charts/AsymmetryGauge.tsx
  - src/app/components/charts/RomTrend.tsx
autonomous: true

must_haves:
  truths:
    - "Asymmetry gauge shows left vs right percentage with visual flagging when imbalance exceeds 10%"
    - "Positive asymmetry indicates right dominance, negative indicates left dominance"
    - "ROM trend chart shows range of motion changes over reps or sessions"
    - "Imbalanced reps are visually distinct from balanced reps (color change)"
  artifacts:
    - path: "src/app/components/charts/AsymmetryGauge.tsx"
      provides: "Dual-gauge or bar visualization for L/R cable asymmetry with 10% threshold flagging"
      exports: ["AsymmetryGauge"]
    - path: "src/app/components/charts/RomTrend.tsx"
      provides: "Range of motion trend line chart"
      exports: ["RomTrend"]
  key_links:
    - from: "src/app/components/charts/AsymmetryGauge.tsx"
      to: "src/lib/biomechanics.ts"
      via: "calculateAsymmetry and ASYMMETRY_THRESHOLD imports"
      pattern: "import.*calculateAsymmetry.*ASYMMETRY_THRESHOLD.*from.*@/lib/biomechanics"
---

<objective>
Build asymmetry detection visualization and ROM analysis components. Asymmetry shows left-vs-right cable force imbalance with threshold flagging; ROM shows range of motion trends across reps.

Purpose: BIO-03, BIO-04, BIO-08 -- asymmetry detection is unique to dual-cable machines like the Vitruvian Trainer. No other consumer app shows this data.
Output: AsymmetryGauge and RomTrend chart components
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-premium-analytics/04-RESEARCH.md
@.planning/phases/04-premium-analytics/04-01-SUMMARY.md
@src/lib/biomechanics.ts
@src/app/components/charts/shared/ChartTheme.ts
@src/app/components/charts/shared/ChartTooltip.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build AsymmetryGauge with L/R visualization and threshold flagging</name>
  <files>src/app/components/charts/AsymmetryGauge.tsx</files>
  <action>
    Create `src/app/components/charts/AsymmetryGauge.tsx`:

    **Props interface `AsymmetryGaugeProps`:**
    - `repSummaries: RepSummary[]` -- rep data with left_force_avg, right_force_avg, asymmetry_pct
    - `height?: number` -- default 300
    - `mode?: 'per-rep' | 'summary'` -- default 'per-rep'. Summary shows aggregate; per-rep shows each rep.

    **Implementation for 'per-rep' mode:**
    1. Wrap in `ParentSize` for responsive width
    2. Horizontal diverging bar chart using visx `<Bar>`:
       - Center line represents 0% asymmetry (perfect balance)
       - For each rep, render a horizontal bar extending left or right from center
       - Bar length proportional to `|asymmetry_pct|`
       - If pre-computed `asymmetry_pct` is available, use it; otherwise compute with `calculateAsymmetry(left_force_avg, right_force_avg)`
       - Color: Forge Green (#10B981) when `|asymmetry| <= ASYMMETRY_THRESHOLD`, Flame Red (#DC2626) when `|asymmetry| > ASYMMETRY_THRESHOLD`
       - Y-axis: rep numbers
       - X-axis: asymmetry percentage with ticks at -20, -10, 0, 10, 20
    3. Draw a dashed vertical line at +10% and -10% as threshold markers (ASYMMETRY_THRESHOLD)
    4. Label each bar with "L+X%" or "R+X%" or "Balanced"
    5. Add a header text: "Left Dominant" on the left side, "Right Dominant" on the right side

    **Implementation for 'summary' mode:**
    1. Calculate average asymmetry across all reps
    2. Render a single large gauge-style display:
       - Circular arc or horizontal bar showing left vs right percentage split
       - Large center number showing asymmetry percentage
       - Color-coded badge: green if balanced, red if imbalanced
       - Text: "Balanced", "Slight Right Imbalance", "Significant Left Imbalance", etc.

    **Shared:**
    - Handle empty repSummaries with "No asymmetry data" message
    - Tooltip on bar hover: rep number, left force avg, right force avg, asymmetry %, zone status
    - Export as named export
  </action>
  <verify>
    `npm run build` succeeds. AsymmetryGauge exports correctly. Imports ASYMMETRY_THRESHOLD and calculateAsymmetry from @/lib/biomechanics.
  </verify>
  <done>
    AsymmetryGauge renders per-rep or summary asymmetry visualization with 10% threshold flagging. Imbalanced reps display in red, balanced in green. Clear L/R dominance labels.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build RomTrend line chart component</name>
  <files>src/app/components/charts/RomTrend.tsx</files>
  <action>
    Create `src/app/components/charts/RomTrend.tsx`:

    **Props interface `RomTrendProps`:**
    - `repSummaries: RepSummary[]` -- rep data with rom_mm field
    - `height?: number` -- default 250
    - `showAverage?: boolean` -- default true, shows horizontal average line

    **Implementation:**
    1. Wrap in `ParentSize`
    2. Line chart using visx `<LinePath>` with `curveMonotoneX`:
       - X-axis: rep number (scaleLinear, domain [1, maxRep])
       - Y-axis: ROM in mm (scaleLinear, domain [min * 0.9, max * 1.1] for headroom)
       - Line color: Gold (#F59E0B)
       - Stroke width 2, with circle markers on each data point (radius 4)
    3. If `showAverage`:
       - Calculate mean ROM across all reps
       - Draw a dashed horizontal line at the average value using `<Line>` from @visx/shape
       - Label the line with "Avg: XXmm" text
    4. Fill area below the line with a subtle gradient (Gold fading to transparent) using `<AreaClosed>`
    5. Axes: AxisBottom with rep numbers, AxisLeft with "ROM (mm)" label
    6. Tooltip on point hover: rep number, ROM value, deviation from average
    7. Handle empty data: "No ROM data available" message
    8. Export as named export
  </action>
  <verify>
    `npm run build` succeeds. RomTrend exports correctly. Uses visx LinePath and AreaClosed.
  </verify>
  <done>
    RomTrend renders per-rep range of motion as a line chart with average reference line and gradient fill. Responsive and handles empty state.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. AsymmetryGauge uses calculateAsymmetry and ASYMMETRY_THRESHOLD from biomechanics (key link)
3. Both per-rep and summary modes render without errors
4. Threshold flagging at 10% uses distinct red vs green colors
5. RomTrend shows average reference line when showAverage is true
6. Both components handle empty arrays gracefully
</verification>

<success_criteria>
- AsymmetryGauge clearly communicates left vs right dominance direction
- Imbalanced reps (>10%) are visually alarming (red) vs balanced (green)
- RomTrend shows exercise consistency via ROM variation across reps
- Both charts are responsive and Phoenix-themed
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-premium-analytics/04-04-SUMMARY.md`
</output>
