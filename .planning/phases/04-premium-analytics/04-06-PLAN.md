---
phase: 04-premium-analytics
plan: 06
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/app/components/ExerciseProgress.tsx
  - src/app/components/SummaryReport.tsx
autonomous: true

must_haves:
  truths:
    - "Exercise progress charts show weight, volume, and estimated 1RM trends over time"
    - "User can select an exercise and see its historical progression"
    - "Weekly/monthly summary reports show volume, frequency, PRs, and consistency score"
    - "Summary reports render as cards with key metrics and mini charts"
  artifacts:
    - path: "src/app/components/ExerciseProgress.tsx"
      provides: "Exercise-level progress charts with weight/volume/1RM trends"
      exports: ["ExerciseProgress"]
    - path: "src/app/components/SummaryReport.tsx"
      provides: "Weekly/monthly summary report cards"
      exports: ["SummaryReport"]
  key_links:
    - from: "src/app/components/ExerciseProgress.tsx"
      to: "src/queries/progress.ts"
      via: "useQuery(exerciseProgressOptions(...))"
      pattern: "useQuery.*exerciseProgressOptions"
    - from: "src/app/components/ExerciseProgress.tsx"
      to: "src/lib/biomechanics.ts"
      via: "estimateOneRepMax for 1RM trend line"
      pattern: "import.*estimateOneRepMax.*from.*@/lib/biomechanics"
---

<objective>
Build exercise-level progress charts and weekly/monthly summary reports. These use Recharts (already installed, not visx) for familiar line/bar trend charts that show how an exercise improves over weeks and months.

Purpose: BIO-10, BIO-13 -- progress visualization and summary reports make subscribers feel the subscription is worth it by showing concrete improvement trends.
Output: ExerciseProgress page component and SummaryReport component
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-premium-analytics/04-RESEARCH.md
@.planning/phases/04-premium-analytics/04-01-SUMMARY.md
@src/app/components/Analytics.tsx
@src/queries/progress.ts
@src/lib/biomechanics.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ExerciseProgress component with weight/volume/1RM trend charts</name>
  <files>src/app/components/ExerciseProgress.tsx</files>
  <action>
    Create `src/app/components/ExerciseProgress.tsx`:

    **Props interface `ExerciseProgressProps`:**
    - `userId: string`
    - `initialExercise?: string` -- pre-selected exercise name

    **Implementation:**
    1. Exercise selector at top: shadcn `Select` dropdown listing all exercises the user has done. Fetch distinct exercise names from exercise_progress table (add a query for this if needed, or derive from the progress data).

    2. Time range selector: tabs for "1 Month", "3 Months", "6 Months", "1 Year", "All Time". Filter data client-side.

    3. Three Recharts `<ResponsiveContainer>` + `<LineChart>` panels in a grid (1 col mobile, 3 col desktop):

       **Panel A: Max Weight Trend**
       - X-axis: date (recorded_at), formatted as "MMM d"
       - Y-axis: weight in kg (max_weight_kg from exercise_progress)
       - Line color: Ember (#FF6B35)
       - Area fill: Ember at 10% opacity
       - Dot on each data point

       **Panel B: Total Volume Trend**
       - X-axis: date
       - Y-axis: volume in kg (total_volume_kg)
       - Line color: Gold (#F59E0B)
       - Area fill: Gold at 10% opacity

       **Panel C: Estimated 1RM Trend**
       - X-axis: date
       - Y-axis: estimated 1RM (estimated_1rm_kg from exercise_progress, or compute client-side with `estimateOneRepMax(max_weight_kg, max_reps)`)
       - Line color: Forge Green (#10B981)
       - Area fill: Green at 10% opacity

    4. Summary stats above charts: current value, change from first to last data point (absolute + percentage), direction arrow (up/down/flat)

    5. Data fetching: `useQuery(exerciseProgressOptions(userId, selectedExercise))` -- uses the query from 04-01

    6. Empty state: "Select an exercise to view progress" or "No progress data for this exercise"

    7. Use existing Recharts components (already in project): `LineChart`, `Line`, `XAxis`, `YAxis`, `CartesianGrid`, `Tooltip`, `ResponsiveContainer`, `Area`, `AreaChart`

    8. Style Recharts to match Phoenix theme:
       - Grid stroke: #1A1A2E
       - Axis tick: #9CA3AF
       - Tooltip: dark background matching theme
       - Use `theme.css` color variables where possible

    9. Export as named export
  </action>
  <verify>
    `npm run build` succeeds. ExerciseProgress exports correctly. Uses Recharts (not visx) for trend charts. Imports exerciseProgressOptions from queries/progress.
  </verify>
  <done>
    ExerciseProgress renders three trend charts (weight, volume, 1RM) for a selected exercise with time range filtering, summary stats, and Phoenix-themed Recharts styling. Responsive grid layout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Build SummaryReport component with weekly/monthly metrics cards</name>
  <files>src/app/components/SummaryReport.tsx</files>
  <action>
    Create `src/app/components/SummaryReport.tsx`:

    **Props interface `SummaryReportProps`:**
    - `userId: string`

    **Implementation:**
    1. Period selector: tabs for "This Week" and "This Month"

    2. Fetch data: `useQuery(weeklySummaryOptions(userId, period))` from queries/progress

    3. Summary cards grid (2 col mobile, 4 col desktop) using shadcn `Card`:

       **Card A: Total Volume**
       - Large number showing total volume in kg for the period
       - Mini sparkline (Recharts `<LineChart>` with no axes, just the line) showing daily volume trend
       - Comparison: "vs last week/month" with percentage change and up/down arrow
       - Icon: dumbbell or weight icon

       **Card B: Workout Frequency**
       - Large number showing workouts completed
       - Mini bar chart showing days worked out
       - Target comparison: "X of 5 target" (hardcode 5 as default target for now)
       - Icon: calendar icon

       **Card C: Personal Records**
       - Large number showing PRs hit in the period
       - List of PR names (e.g., "Bench Press +2.5kg")
       - Use records query to find PRs within date range
       - Icon: trophy icon

       **Card D: Consistency Score**
       - Large number showing a 0-100 consistency score
       - Computed: `(workoutDays / targetDays) * 100`, capped at 100
       - Color: Red (<50), Gold (50-80), Green (>80)
       - Mini progress ring (SVG circle with stroke-dasharray) showing percentage
       - Icon: fire/streak icon

    4. Below cards: a brief "Highlights" section with bullet points:
       - Best session by volume
       - Most improved exercise (biggest weight increase)
       - Longest streak in the period

    5. Style: cards with dark backgrounds (#1A1A2E), subtle borders (#2D2D44), Phoenix accent colors for key numbers

    6. Loading: skeleton cards while data loads
    7. Empty: "Complete some workouts to see your summary" message

    8. Export as named export
  </action>
  <verify>
    `npm run build` succeeds. SummaryReport exports correctly. Uses Recharts for mini sparklines. Uses shadcn Card components.
  </verify>
  <done>
    SummaryReport renders 4 metric cards (volume, frequency, PRs, consistency) with sparklines, comparison stats, and highlights section. Weekly and monthly views. Phoenix-themed dark cards.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. ExerciseProgress uses Recharts (not visx) for trend line charts
3. ExerciseProgress fetches from exerciseProgressOptions query (key link)
4. SummaryReport computes consistency score from workout frequency
5. Both components handle empty data states gracefully
6. Phoenix theme colors applied to all chart elements
7. Responsive layouts work on mobile and desktop
</verification>

<success_criteria>
- ExerciseProgress shows clear improvement trends for any exercise
- 1RM estimation uses Epley formula from biomechanics lib
- SummaryReport gives a quick weekly/monthly overview with actionable highlights
- Mini charts in summary cards provide visual context without overwhelming
- Build passes clean
</success_criteria>

<output>
After completion, create `.planning/phases/04-premium-analytics/04-06-SUMMARY.md`
</output>
