---
phase: 09-foundation-toolchain
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/components/ui/calendar.tsx
  - src/app/components/ui/chart.tsx
  - src/app/components/RoutineBuilder.tsx
  - src/main.tsx
  - vite.config.ts
  - tsconfig.app.json
  - .nvmrc
autonomous: true
requirements: [TOOL-02, TOOL-03, TOOL-04, TOOL-05, TOOL-06, TOOL-07]

must_haves:
  truths:
    - "npm run build completes with zero TypeScript errors on React 19 + Vite 7"
    - "Calendar component renders correctly with react-day-picker v9 API"
    - "All chart components render correctly with Recharts 3"
    - "Drag-and-drop in RoutineBuilder works with updated dnd-kit package"
    - "npm run test runs without Vite/React version incompatibility errors"
  artifacts:
    - path: "package.json"
      provides: "Updated dependency versions"
      contains: "react.*19"
    - path: "src/app/components/ui/calendar.tsx"
      provides: "react-day-picker v9 compatible Calendar"
      contains: "Chevron"
    - path: "src/app/components/ui/chart.tsx"
      provides: "Recharts 3 compatible chart primitives"
    - path: ".nvmrc"
      provides: "Node.js version pinning"
  key_links:
    - from: "package.json"
      to: "vite.config.ts"
      via: "Vite 7 + @vitejs/plugin-react 5.x"
      pattern: "plugin-react"
    - from: "package.json"
      to: "src/app/components/ui/calendar.tsx"
      via: "react-day-picker v9 API"
      pattern: "react-day-picker"
    - from: "package.json"
      to: "src/main.tsx"
      via: "React 19 createRoot"
      pattern: "react-dom.*19"
---

<objective>
Upgrade all major dependencies in the strict prescribed sequence: react-day-picker v9 -> Tailwind 4.1.18 -> Vite 7 -> Recharts 3 -> dnd-kit -> React 19. Each step must build successfully before proceeding to the next.

Purpose: Modernize the runtime stack to React 19 + Vite 7, resolving the pre-existing dnd-kit version mismatch and enabling React 19 ecosystem features needed by Sentry v10 and future phases.
Output: A buildable, testable project on React 19.2.x + Vite 7.x with all charting, calendar, and drag-and-drop components migrated to their latest major versions.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-foundation-toolchain/09-RESEARCH.md
@package.json
@vite.config.ts
@tsconfig.app.json
@src/main.tsx
@src/app/components/ui/calendar.tsx
@src/app/components/ui/chart.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Upgrade react-day-picker v9, Tailwind 4.1.18, and Vite 7</name>
  <files>package.json, src/app/components/ui/calendar.tsx, vite.config.ts, .nvmrc, tsconfig.app.json</files>
  <action>
**Step 1 — react-day-picker v9:**
Run: `npm install react-day-picker@latest`

Rewrite `src/app/components/ui/calendar.tsx` for v9 API:
- Replace `IconLeft`/`IconRight` custom components with a single `Chevron` component
- Rename all classNames: `cell` -> `day`, `day` -> `day_button`, `caption` -> `month_caption`, `caption_label` -> `month_caption`, `nav_button` -> `button_previous`/`button_next`, `nav_button_previous` -> `button_previous`, `nav_button_next` -> `button_next`, `head_row` -> `weekdays`, `head_cell` -> `weekday`, `row` -> `week`, `table` -> `months`
- Rename props: `fromMonth` -> `startMonth`, `toMonth` -> `endMonth` (if used)
- Use the shadcn v9-compatible Calendar pattern. Reference: https://ui.shadcn.com/docs/changelog/2025-06-calendar

Run `npm run build` to verify. Fix any TypeScript errors.

**Step 2 — Tailwind 4.1.18:**
Run: `npm install tailwindcss@4.1.18 @tailwindcss/vite@4.1.18`
(Research indicates current version is 4.1.12, needs upgrade for Vite 7 peer dep support.)

Run `npm run build` to verify. Should be a clean upgrade.

**Step 3 — Vite 7 + plugin-react 5.x:**
Run: `npm install vite@^7.3.1 @vitejs/plugin-react@^5.1.4`

In `vite.config.ts`:
- Update the import: `/// <reference types="vitest/config" />` may need adjustment — verify Vitest 4.0.18 works with Vite 7 (research confirms it does).
- The `manualChunks` config is fine for Vite 7. No `splitVendorChunkPlugin` to remove (project already uses manualChunks).

Create `.nvmrc` with content: `22` (Node 22.x — project runs on Node 24.5.0 which exceeds Vite 7 minimum of 20.19+).

Add `engines` field to `package.json`: `"engines": { "node": ">=20.19.0" }`

Run `npm run build` to verify.
Run `npm run test` to verify Vitest still works.
  </action>
  <verify>
Run `npm run build` — must complete with zero errors.
Run `npm run test` — must run without Vite compatibility errors (pre-existing test failures are acceptable).
Verify `package.json` shows: react-day-picker@^9.x, tailwindcss@4.1.18, @tailwindcss/vite@4.1.18, vite@^7.x, @vitejs/plugin-react@^5.x.
Verify `.nvmrc` exists with `22`.
  </verify>
  <done>react-day-picker v9, Tailwind 4.1.18, and Vite 7 installed and building. Calendar component uses v9 Chevron API. .nvmrc and engines field present.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade Recharts 3, dnd-kit, and React 19</name>
  <files>package.json, src/app/components/ui/chart.tsx, src/app/components/RoutineBuilder.tsx, src/main.tsx, tsconfig.app.json</files>
  <action>
**Step 4 — Recharts 3:**
Run: `npm install recharts@^3.7.0`

In `src/app/components/ui/chart.tsx`:
- Rename `TooltipProps` import to `TooltipContentProps` (Recharts 3 renamed this type).
- Verify `ResponsiveContainer` import still works (it does in Recharts 3).
- Check for any `CategoricalChartState` usage — if found, remove/replace (research says it was removed in v3).

Grep all files importing from `recharts` and fix any breaking changes:
- `import { ... } from 'recharts'` — check each import is still valid in v3
- `accessibilityLayer` now defaults to `true` — no action needed unless it causes issues

Run `npm run build` to verify.

**Step 5 — dnd-kit migration:**
Run: `npm uninstall @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities`
Run: `npm install @dnd-kit/react@0.3.0 @dnd-kit/helpers`

Note: Pin exact version `0.3.0` for @dnd-kit/react (pre-1.0 package — avoid caret range).

Find all files importing from `@dnd-kit/core`, `@dnd-kit/sortable`, or `@dnd-kit/utilities`. Research says only RoutineBuilder.tsx uses dnd-kit.

Migrate RoutineBuilder.tsx:
- Replace `import { DndContext, closestCenter, DragEndEvent } from '@dnd-kit/core'` with `import { DragDropProvider } from '@dnd-kit/react'`
- Replace `import { arrayMove, SortableContext, useSortable, verticalListSortingStrategy } from '@dnd-kit/sortable'` with `import { useSortable } from '@dnd-kit/react/sortable'` and `import { move } from '@dnd-kit/helpers'`
- Remove `import { CSS } from '@dnd-kit/utilities'`
- Update component JSX: `<DndContext>` -> `<DragDropProvider>`, remove `<SortableContext>` wrapper (not needed in v7)
- Update `useSortable` hook usage per new API
- If migration proves too complex or @dnd-kit/react is unstable, FALLBACK: keep @dnd-kit/core@6 + @dnd-kit/sortable@10 and add `--legacy-peer-deps` note. The version mismatch is cosmetic if both work.

Run `npm run build` to verify.

**Step 6 — React 19:**
Run: `npm install react@^19.2.4 react-dom@^19.2.4`
Run: `npm install -D @types/react@^19.0.0 @types/react-dom@^19.0.0`

React 19 changes to verify:
- `createRoot` API is unchanged — `src/main.tsx` should work as-is
- `forwardRef` is deprecated but project has zero usages (safe)
- `ref` is now a regular prop — no action needed (backward compatible)

Run `npm run build` — fix any TypeScript errors from @types/react@19 changes.
Common issues: `ReactNode` type changes, `PropsWithChildren` changes. Fix each as encountered.

Run `npm run test` to verify test suite still runs.
  </action>
  <verify>
Run `npm run build` — must complete with zero TypeScript errors.
Run `npm run test` — must run (pre-existing failures acceptable, no NEW failures).
Verify `package.json` shows: recharts@^3.x, @dnd-kit/react@0.3.0, react@^19.x, react-dom@^19.x, @types/react@^19.x.
Verify @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities are NOT in package.json (or if fallback was used, document why).
  </verify>
  <done>Recharts 3, dnd-kit unified package, and React 19 installed and building. All chart components, drag-and-drop, and React rendering work on the new stack. No new test failures introduced.</done>
</task>

</tasks>

<verification>
1. `npm run build` completes with zero TypeScript errors
2. `npm run test` runs without version incompatibility errors (pre-existing test failures are acceptable)
3. `package.json` dependencies show: react@19.x, react-dom@19.x, vite@7.x, recharts@3.x, react-day-picker@9.x
4. `package.json` does NOT contain: @dnd-kit/core, @dnd-kit/sortable, @dnd-kit/utilities (unless fallback was used)
5. `.nvmrc` exists with Node version
6. `package.json` has `engines.node` field
7. Main entry chunk size is within 10% of 70.76 KB baseline (63.7-77.8 KB)
</verification>

<success_criteria>
The project builds and runs on React 19.2.x + Vite 7.x with Recharts 3, react-day-picker v9, and unified dnd-kit. Zero TypeScript errors. All components render. Bundle size within 10% of v1.0 baseline.
</success_criteria>

<output>
After completion, create `.planning/phases/09-foundation-toolchain/09-01-SUMMARY.md`
</output>
