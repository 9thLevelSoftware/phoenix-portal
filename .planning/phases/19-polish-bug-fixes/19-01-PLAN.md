---
phase: 19-polish-bug-fixes
plan: 01
type: execute
wave: 1
depends_on: []
autonomous: true
files_modified:
  - src/app/components/LandingPage.tsx
  - src/app/components/Dashboard.tsx
requirements: [BUG-09, BUG-10]
must_haves:
  truths:
    - "All footer nav <li> elements are wrapped in <Link> or <a> tags with proper href"
    - "animate-flame-flicker and animate-phoenix-glow CSS classes are applied to at least one visible UI element each"
  artifacts:
    - path: "src/app/components/LandingPage.tsx"
      provides: "Footer nav items with proper link wrapping"
    - path: "src/app/components/Dashboard.tsx"
      provides: "CSS animations wired to relevant UI elements"
---

<objective>
Two surgical fixes: wrap footer nav `<li>` elements in proper Link/anchor tags, and wire the unused `animate-flame-flicker` and `animate-phoenix-glow` CSS animations to relevant UI elements.

Purpose: Footer links must be proper anchors for accessibility (right-click context menu, keyboard navigation). Unused CSS animations are dead code — either wire them or delete them; wiring adds brand polish.

Output: 2 modified files; build passes.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/19-polish-bug-fixes/19-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wrap footer nav items in proper Link/anchor tags</name>
  <files>
src/app/components/LandingPage.tsx
  </files>
  <action>
Find the footer section in LandingPage.tsx (around lines 889-987). Several `<li>` elements have `hover:text-primary cursor-pointer` classes but no `<Link>` or `<a>` wrapping.

**Fix each unwrapped `<li>`:**

For items that link to internal routes (e.g., Dashboard, Analytics, Community):
```tsx
<li><Link to="/dashboard" className="hover:text-primary transition-colors">Dashboard</Link></li>
```

For items that link to internal public routes (Privacy, Terms, FAQ):
```tsx
<li><Link to="/privacy" className="hover:text-primary transition-colors">Privacy Policy</Link></li>
```

For items that are purely informational with no real destination (e.g., "Roadmap", "Security", "Integrations" — pages that don't exist yet):
```tsx
<li><span className="text-muted-foreground/50 cursor-default">Roadmap</span></li>
```
Remove the `hover:text-primary cursor-pointer` from items with no valid destination — don't create dead links.

For external links (if any, like "Support on Ko-fi"):
```tsx
<li><a href="https://..." target="_blank" rel="noopener noreferrer" className="hover:text-primary transition-colors">Ko-fi</a></li>
```

Check the existing route definitions in `src/app/routes/index.tsx` to determine which routes actually exist before creating Link targets. Known existing routes include: /dashboard, /analytics, /workouts, /community, /routines, /challenges, /profile, /privacy, /terms, /faq.

The `Link` component should already be imported from `react-router` in LandingPage.tsx.
  </action>
  <verify>
1. Check that no `<li>` with `cursor-pointer` exists without a child `<Link>` or `<a>`: `grep -n "cursor-pointer" src/app/components/LandingPage.tsx` in footer section
2. `npm run typecheck` — should pass
  </verify>
  <done>All footer nav items use proper Link/anchor tags or are styled as non-interactive for items with no valid destination.</done>
</task>

<task type="auto">
  <name>Task 2: Wire unused CSS animations to relevant UI elements</name>
  <files>
src/app/components/Dashboard.tsx
  </files>
  <action>
The CSS animations `animate-flame-flicker` and `animate-phoenix-glow` are defined in `src/styles/theme.css` but not applied anywhere in the codebase.

**Wire them to appropriate elements:**

1. **`animate-flame-flicker`** — Apply to the streak card's Flame icon on the Dashboard. Find the Lucide `<Flame>` icon in the streak display section and add the class:
```tsx
<Flame className="w-5 h-5 text-primary animate-flame-flicker" />
```
This makes the streak flame icon subtly flicker — reinforcing the "keep your streak alive" metaphor.

2. **`animate-phoenix-glow`** — Apply to the Dashboard's hero/welcome card or the streak count glow effect. Find a prominent branded element and add:
```tsx
<div className="... animate-phoenix-glow">
```
This creates a subtle pulsing glow around the element.

**Key details:**
- These are CSS keyframe animations (not Framer Motion) — they work via Tailwind utility classes
- The `prefers-reduced-motion` media query in theme.css already suppresses these animations
- Apply sparingly — max 1-2 elements per animation
- Check theme.css first to understand what each animation does before applying
  </action>
  <verify>
1. `grep -rn "animate-flame-flicker" src/app/components/` — should find at least 1 usage
2. `grep -rn "animate-phoenix-glow" src/app/components/` — should find at least 1 usage
3. `npm run typecheck` — should pass
4. `npm run build` — should pass
  </verify>
  <done>animate-flame-flicker applied to streak Flame icon; animate-phoenix-glow applied to a prominent Dashboard element.</done>
</task>

</tasks>

<verification>
1. No `<li>` with `cursor-pointer` lacking a child Link/anchor in footer
2. `grep -rn "animate-flame-flicker" src/app/components/` — at least 1 match
3. `grep -rn "animate-phoenix-glow" src/app/components/` — at least 1 match
4. `npm run typecheck` — zero errors
5. `npm run build` — passes
6. `npm test` — passes
</verification>

<success_criteria>
- Right-clicking any footer nav item shows browser link context menu options
- animate-flame-flicker and animate-phoenix-glow are each applied to at least one visible element
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/19-polish-bug-fixes/19-01-SUMMARY.md`
</output>
