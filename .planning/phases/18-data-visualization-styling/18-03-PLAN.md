---
phase: 18-data-visualization-styling
plan: 03
type: execute
wave: 2
depends_on: [18-01]
autonomous: false
agent: engineering-frontend-developer
files_modified:
  - src/app/components/MuscleHeatmap.tsx
  - src/app/components/ExerciseProgress.tsx
requirements: [VIZ-07, VIZ-08]
must_haves:
  truths:
    - "Muscle heatmap shows both front and back body regions with correct SVG paths"
    - "ExerciseProgress stat values use text-4xl font-bold with color-coded delta pill"
  artifacts:
    - path: "src/app/components/MuscleHeatmap.tsx"
      provides: "Front/back toggle with anatomically correct SVG paths for both views"
    - path: "src/app/components/ExerciseProgress.tsx"
      provides: "Larger stat values with delta pill showing improvement direction"
---

<objective>
Agent: engineering-frontend-developer

Fix the muscle heatmap to show both front and back body regions, and upgrade ExerciseProgress stat values to be larger with color-coded delta indicators.

Purpose: The heatmap currently only shows front-facing muscles — back muscles (lats, traps, glutes, hamstrings) are invisible. ExerciseProgress stats need visual prominence.

Output: 2 modified files; heatmap has front+back, stats are larger with deltas.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/18-data-visualization-styling/18-CONTEXT.md
@.planning/phases/18-data-visualization-styling/18-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add front/back toggle to MuscleHeatmap with back body SVG paths</name>
  <files>
src/app/components/MuscleHeatmap.tsx
  </files>
  <action>
Read `src/app/components/MuscleHeatmap.tsx` first to understand the current structure.

**Current state:** Only front-facing SVG body outline with regions for Shoulders, Chest, Arms, Core, Legs, Back (minimal lats representation from front).

**Changes:**

1. Add a `useState` for view toggle:
```tsx
const [view, setView] = useState<"front" | "back">("front");
```

2. Add toggle buttons above the SVG:
```tsx
<div className="flex gap-1 mb-3">
  <button
    onClick={() => setView("front")}
    className={`px-3 py-1 text-xs rounded-md transition-colors ${
      view === "front"
        ? "bg-primary/20 text-primary"
        : "text-muted-foreground hover:text-foreground"
    }`}
  >
    Front
  </button>
  <button
    onClick={() => setView("back")}
    className={`px-3 py-1 text-xs rounded-md transition-colors ${
      view === "back"
        ? "bg-primary/20 text-primary"
        : "text-muted-foreground hover:text-foreground"
    }`}
  >
    Back
  </button>
</div>
```

3. Define back body SVG paths for these muscle regions:
   - **Traps** (upper back/neck area)
   - **Lats** (mid-back, wide V-shape)
   - **Lower Back** (lumbar region)
   - **Glutes** (buttocks)
   - **Hamstrings** (back of thighs)
   - **Calves** (back of lower legs)

The SVG paths should mirror the front view's body outline proportions. Use the same viewBox dimensions as the front SVG. The paths should be anatomically reasonable — proportional ovals/shapes in correct positions, similar to a simple anatomy diagram.

4. Conditionally render front or back paths based on `view` state:
```tsx
{view === "front" ? (
  <>{/* existing front paths */}</>
) : (
  <>{/* new back paths */}</>
)}
```

5. The heatmap coloring logic (opacity based on muscle group volume) should work for both views — map the back muscle regions to the existing data categories:
   - Traps → "Shoulders" data
   - Lats → "Back" data
   - Lower Back → "Back" data
   - Glutes → "Legs" data
   - Hamstrings → "Legs" data
   - Calves → "Legs" data

6. Fix the tooltip background — replace hardcoded `#1F2937` with `var(--surface-2)` if present.
  </action>
  <verify>
1. `grep -n "front.*back\|view.*front\|setView" src/app/components/MuscleHeatmap.tsx` — toggle state exists
2. `grep -c "Traps\|Lats\|Hamstrings\|Glutes" src/app/components/MuscleHeatmap.tsx` — back muscle regions defined
3. `grep -c "#1F2937" src/app/components/MuscleHeatmap.tsx` — should be 0 (replaced with CSS var)
4. `npm run typecheck` — should pass
  </verify>
  <done>Muscle heatmap has front/back toggle with anatomically correct SVG paths for both views; tooltip uses CSS variable.</done>
</task>

<task type="auto">
  <name>Task 2: Upgrade ExerciseProgress stat values with larger text and delta pill</name>
  <files>
src/app/components/ExerciseProgress.tsx
  </files>
  <action>
Read `src/app/components/ExerciseProgress.tsx` first.

**Current state:** Stat values use `text-2xl font-semibold` with a dynamic color prop.

**Changes:**

1. Find the stat value display (around line 97-131, the `<div className="text-2xl font-semibold">` element).

2. Increase size and weight:
```tsx
<div className="text-4xl font-bold tabular-nums" style={{ color }}>
  {stat.current} {unit}
</div>
```

3. Add a color-coded delta pill below each stat value. The delta shows improvement from previous period:

```tsx
{stat.delta !== undefined && stat.delta !== 0 && (
  <span
    className={`inline-flex items-center gap-0.5 rounded-full px-2 py-0.5 text-xs font-medium ${
      stat.delta > 0
        ? "bg-emerald-500/15 text-emerald-400"
        : "bg-red-500/15 text-red-400"
    }`}
  >
    {stat.delta > 0 ? "↑" : "↓"} {Math.abs(stat.delta)}
    {stat.deltaUnit ?? "%"}
  </span>
)}
```

If the stat data structure doesn't have a `delta` field, add it to the interface and compute it from available data (e.g., comparing current vs previous period values). If no previous data exists, simply skip the delta pill.

4. If stat interface changes are needed, update the type and any callers.
  </action>
  <verify>
1. `grep -n "text-4xl font-bold" src/app/components/ExerciseProgress.tsx` — larger stat values
2. `grep -n "delta\|emerald\|pill" src/app/components/ExerciseProgress.tsx` — delta pill rendering
3. `npm run typecheck` — should pass
4. `npm run build` — should pass
  </verify>
  <done>ExerciseProgress stats use text-4xl font-bold with color-coded delta pills showing improvement direction.</done>
</task>

</tasks>

<verification>
1. `grep -n "setView\|front.*back" src/app/components/MuscleHeatmap.tsx` — front/back toggle
2. `grep -c "text-4xl" src/app/components/ExerciseProgress.tsx` — larger stats
3. `npm run typecheck` — zero errors
4. `npm run build` — passes
5. `npm test` — passes
</verification>

<success_criteria>
- Muscle heatmap shows both front and back body with correct SVG paths — toggled by front/back buttons
- ExerciseProgress stat values are visually prominent (text-4xl font-bold) with delta pills
- No broken SVG paths — regions are visible and colored by workout data
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/18-data-visualization-styling/18-03-SUMMARY.md`
</output>
