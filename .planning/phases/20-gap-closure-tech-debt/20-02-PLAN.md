---
phase: 20-gap-closure-tech-debt
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/components/LandingPage.tsx
  - src/app/components/Navigation.tsx
  - src/app/components/MobileBottomNav.tsx
autonomous: true
requirements: [TYPE-03]
must_haves:
  truths:
    - "LandingPage hero h1 has no inline style prop — font-family inherits from global CSS"
    - "Navigation.tsx does not exist in the codebase"
    - "MobileBottomNav drawer section labels use .eyebrow utility class"
  artifacts:
    - path: "src/app/components/LandingPage.tsx"
      provides: "Hero h1 without inline fontFamily override"
    - path: "src/app/components/MobileBottomNav.tsx"
      provides: "Drawer section labels using .eyebrow utility"
  key_links:
    - from: "src/styles/fonts.css"
      to: "src/app/components/LandingPage.tsx hero h1"
      via: "Global Inter font-family declaration via @theme block"
      pattern: "font-family.*Inter"
    - from: "src/styles/theme.css (.eyebrow)"
      to: "src/app/components/MobileBottomNav.tsx section labels"
      via: "CSS class application"
      pattern: "className.*eyebrow"
---

<objective>
Three surgical cleanup fixes: remove the last inline system-ui fontFamily fallback from LandingPage hero h1, delete the dead Navigation.tsx file, and replace manual Tailwind classes with the canonical .eyebrow utility on MobileBottomNav drawer section labels.

Purpose: These are leftover artifacts from Phases 14-15 that were deferred to this gap closure phase. Each is a one-location fix that eliminates tech debt and ensures design system consistency.

Output: 2 modified files, 1 deleted file; build passes.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-gap-closure-tech-debt/20-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove inline fontFamily from LandingPage hero h1, delete Navigation.tsx, apply eyebrow utility</name>
  <files>
src/app/components/LandingPage.tsx
src/app/components/Navigation.tsx
src/app/components/MobileBottomNav.tsx
  </files>
  <action>
Three changes:

**1. LandingPage.tsx (line ~551):** Remove the `style={{ fontFamily: "Inter, system-ui, sans-serif" }}` prop from the `<motion.h1>` element. Phase 14 loaded Inter globally via fonts.css @theme block — the inline style is redundant and prevents the CSS cascade from working correctly. Keep all other props (className, initial, animate, transition) unchanged.

BEFORE:
```tsx
<motion.h1
  className="mt-8 text-5xl sm:text-6xl md:text-7xl lg:text-8xl tracking-tight"
  style={{ fontFamily: "Inter, system-ui, sans-serif" }}
  initial={{ opacity: 0 }}
```

AFTER:
```tsx
<motion.h1
  className="mt-8 text-5xl sm:text-6xl md:text-7xl lg:text-8xl tracking-tight"
  initial={{ opacity: 0 }}
```

**2. Navigation.tsx:** Delete the file entirely. It was deprecated in Phase 15-01 (replaced by AppSidebar). Verified safe to delete: no imports found in codebase (grep confirms only SetNavigation, NavigationMenu, and comments reference "Navigation" — none import the Navigation component itself).

**3. MobileBottomNav.tsx (line ~243):** Replace manual Tailwind classes with the `.eyebrow` utility on the drawer section label `<p>` elements.

BEFORE:
```tsx
<p className="text-xs font-semibold uppercase tracking-widest text-muted-foreground px-4 pt-4 pb-1">
```

AFTER:
```tsx
<p className="eyebrow text-muted-foreground px-4 pt-4 pb-1">
```

WHY `.eyebrow` instead of manual: `.eyebrow` (defined in theme.css @layer base) uses Inter Variable weight 450, 11px font size, and 0.08em letter-spacing. The manual classes use font-semibold (600), text-xs (12px), and tracking-widest (~0.1em) — close but wrong. The canonical utility ensures consistency with all other eyebrow labels in the app.
  </action>
  <verify>
1. `grep -n "system-ui" src/app/components/LandingPage.tsx` — should return zero matches.
2. `ls src/app/components/Navigation.tsx 2>/dev/null && echo "STILL EXISTS" || echo "DELETED"` — should say DELETED.
3. `grep -n "eyebrow" src/app/components/MobileBottomNav.tsx` — should return at least one match.
4. `grep -n "text-xs font-semibold uppercase tracking-widest" src/app/components/MobileBottomNav.tsx` — should return zero matches.
5. `npm run build` — must pass with zero errors (confirms no broken imports from Navigation.tsx deletion).
  </verify>
  <done>LandingPage hero h1 has no inline style prop. Navigation.tsx is deleted. MobileBottomNav section labels use .eyebrow utility. Build passes.</done>
</task>

</tasks>

<verification>
1. `grep -rn "system-ui" src/app/components/LandingPage.tsx` — zero matches
2. Navigation.tsx does not exist: `test -f src/app/components/Navigation.tsx && echo FAIL || echo PASS`
3. `grep -n "eyebrow" src/app/components/MobileBottomNav.tsx` — shows eyebrow class usage
4. `npm run build` passes with zero errors
</verification>

<success_criteria>
- LandingPage hero h1 renders with no inline style prop — font inherits from global CSS
- Navigation.tsx deleted — zero dead nav files
- MobileBottomNav drawer section labels use .eyebrow instead of manual Tailwind
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/20-gap-closure-tech-debt/20-02-SUMMARY.md`
</output>
