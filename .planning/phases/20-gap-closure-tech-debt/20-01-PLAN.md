---
phase: 20-gap-closure-tech-debt
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/routes/AppLayout.tsx
  - src/app/components/Analytics.tsx
  - src/app/components/CelebrationDemo.tsx
  - src/app/components/Challenges.tsx
  - src/app/components/Community.tsx
  - src/app/components/ComparisonView.tsx
  - src/app/components/CycleBuilder.tsx
  - src/app/components/Dashboard.tsx
  - src/app/components/Goals.tsx
  - src/app/components/PersonalRecords.tsx
  - src/app/components/Profile.tsx
  - src/app/components/Recovery.tsx
  - src/app/components/RoutineBuilder.tsx
  - src/app/components/RoutinesEnhanced.tsx
  - src/app/components/SessionDetail.tsx
  - src/app/components/TrainingCycles.tsx
  - src/app/components/WorkoutHistory.tsx
autonomous: true
requirements: [VIS-01]
must_haves:
  truths:
    - "Ambient ember/flame glows are visually perceptible behind authenticated page content"
    - "Glassmorphism cards blur against the ambient glow layer, not just adjacent page content"
    - "Non-authenticated pages (LandingPage, PrivacyPolicy, ResetPassword) still have their own opaque background"
    - "Sticky headers with bg-background/95 retain their frosted glass effect"
  artifacts:
    - path: "src/app/routes/AppLayout.tsx"
      provides: "Transparent shell div and SidebarInset override"
      contains: "bg-transparent"
    - path: "src/app/components/Dashboard.tsx"
      provides: "Transparent root wrappers (representative of all 16 page files)"
  key_links:
    - from: "body::before (theme.css)"
      to: "AppLayout shell div"
      via: "Transparent background allows glow through"
      pattern: "min-h-screen(?!.*bg-background)"
    - from: "AppLayout SidebarInset"
      to: "Page component root wrappers"
      via: "Both transparent, glow visible through entire stack"
      pattern: "bg-transparent"
---

<objective>
Strip opaque `bg-background` from the authenticated app shell and all 16 authenticated page component root wrappers so the body::before ambient ember/flame glows (defined in Phase 14) are visible through the content layers.

Purpose: The ambient glow was built in Phase 14-02 but has been fully occluded by three opaque layers (AppLayout, SidebarInset, page root wrappers) ever since. Removing these redundant backgrounds reveals the glow and enables glassmorphism cards to blur against actual visual depth.

Output: 17 modified files with transparent root wrappers; build passes; glow visible.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-gap-closure-tech-debt/20-RESEARCH.md
@.planning/phases/14-css-foundation-typography/14-02-SUMMARY.md
@.planning/phases/15-navigation-layout-shell/15-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Strip bg-background from AppLayout shell and SidebarInset</name>
  <files>src/app/routes/AppLayout.tsx</files>
  <action>
Two changes in AppLayout.tsx:

1. On the shell div (line ~49), remove ` bg-background` from the className string:
   - BEFORE: `className="min-h-screen bg-background relative z-[10] flex w-full"`
   - AFTER: `className="min-h-screen relative z-[10] flex w-full"`
   - Keep `relative z-[10]` — this positions content above the fixed glow layers. Only the opaque background must go.

2. On the `<SidebarInset>` element (line ~56), add `className="bg-transparent"` to override the component's built-in `bg-background`:
   - BEFORE: `<SidebarInset>`
   - AFTER: `<SidebarInset className="bg-transparent">`
   - Do NOT edit `src/app/components/ui/sidebar.tsx` (the shadcn primitive). Override from the call site only.

WHY: The body already applies `bg-background` (#0D0D0D) via `@apply bg-background` in theme.css `@layer base`. These child-level backgrounds are redundant and block the body::before glow from showing through.
  </action>
  <verify>
Run `grep -n "bg-background" src/app/routes/AppLayout.tsx` — should return zero matches for the opaque pattern. The `bg-transparent` on SidebarInset should be the only background-related class.
Run `npm run build` — must pass.
  </verify>
  <done>AppLayout shell div has no bg-background; SidebarInset overridden with bg-transparent; build passes.</done>
</task>

<task type="auto">
  <name>Task 2: Strip bg-background from all 16 authenticated page root wrappers</name>
  <files>
src/app/components/Analytics.tsx
src/app/components/CelebrationDemo.tsx
src/app/components/Challenges.tsx
src/app/components/Community.tsx
src/app/components/ComparisonView.tsx
src/app/components/CycleBuilder.tsx
src/app/components/Dashboard.tsx
src/app/components/Goals.tsx
src/app/components/PersonalRecords.tsx
src/app/components/Profile.tsx
src/app/components/Recovery.tsx
src/app/components/RoutineBuilder.tsx
src/app/components/RoutinesEnhanced.tsx
src/app/components/SessionDetail.tsx
src/app/components/TrainingCycles.tsx
src/app/components/WorkoutHistory.tsx
  </files>
  <action>
For each of the 16 authenticated page files listed above, remove ` bg-background` from every `min-h-screen bg-background` root wrapper div. The pattern is always a className containing `min-h-screen bg-background` followed by other classes.

Instance counts per file (from research — verify each):
- Analytics.tsx: 2 instances
- CelebrationDemo.tsx: 1 instance
- Challenges.tsx: 2 instances
- Community.tsx: 1 instance
- ComparisonView.tsx: 6 instances (loading/empty states + main return)
- CycleBuilder.tsx: 2 instances
- Dashboard.tsx: 2 instances
- Goals.tsx: 2 instances
- PersonalRecords.tsx: 3 instances
- Profile.tsx: 1 instance
- Recovery.tsx: 3 instances
- RoutineBuilder.tsx: 2 instances
- RoutinesEnhanced.tsx: 2 instances
- SessionDetail.tsx: 3 instances
- TrainingCycles.tsx: 3 instances
- WorkoutHistory.tsx: 3 instances

CRITICAL — DO NOT TOUCH these files (they are outside the authenticated AppLayout boundary):
- LandingPage.tsx (has its own bg-background for the public page)
- PrivacyPolicy.tsx (public page)
- ResetPassword.tsx (public page)

CRITICAL — DO NOT TOUCH sticky headers with `bg-background/95`:
- These have the `/95` opacity modifier and are intentionally semi-transparent for frosted header effects. Only target root wrapper divs with the exact pattern `min-h-screen bg-background` (no opacity modifier).

The replacement is simple string removal: replace ` bg-background` with nothing (empty string) in each className. Keep `min-h-screen` and all other classes intact.
  </action>
  <verify>
After all edits, run these verification commands:
1. `grep -rn "min-h-screen bg-background" src/app/components/ --include="*.tsx"` — should return ONLY LandingPage.tsx, PrivacyPolicy.tsx, and ResetPassword.tsx (the 3 non-authenticated files).
2. `grep -c "bg-background/95" src/app/components/Analytics.tsx` — should still show matches (sticky headers preserved).
3. `npm run build` — must pass with zero errors.
  </verify>
  <done>All 38 instances of `min-h-screen bg-background` removed from 16 authenticated page files. Only LandingPage, PrivacyPolicy, ResetPassword retain their bg-background. Sticky headers with bg-background/95 are untouched. Build passes.</done>
</task>

</tasks>

<verification>
1. `grep -rn "min-h-screen bg-background" src/app/components/ --include="*.tsx"` returns exactly 3 files (LandingPage, PrivacyPolicy, ResetPassword)
2. `grep -n "bg-background" src/app/routes/AppLayout.tsx` returns zero matches
3. `grep -n "bg-transparent" src/app/routes/AppLayout.tsx` returns SidebarInset line
4. `npm run build` passes with zero errors
5. `grep -rn "bg-background/95" src/app/components/ --include="*.tsx"` returns unchanged sticky header matches
</verification>

<success_criteria>
- Zero `min-h-screen bg-background` instances in authenticated page files
- AppLayout shell is transparent (no bg-background)
- SidebarInset overridden with bg-transparent
- Non-authenticated pages (LandingPage, PrivacyPolicy, ResetPassword) retain bg-background
- Sticky headers retain bg-background/95
- Build passes
</success_criteria>

<output>
After completion, create `.planning/phases/20-gap-closure-tech-debt/20-01-SUMMARY.md`
</output>
