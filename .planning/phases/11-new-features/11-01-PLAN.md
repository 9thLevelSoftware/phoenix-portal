---
phase: 11-new-features
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - supabase/migrations/20260217_phase11_goals.sql
  - src/lib/database.types.ts
  - src/schemas/goals.ts
  - src/queries/keys.ts
  - src/queries/goals.ts
  - src/mutations/goals.ts
  - src/app/components/Goals.tsx
  - src/app/components/GoalProgressRing.tsx
  - src/app/components/GoalCelebration.tsx
  - src/app/components/GoalDashboardWidget.tsx
  - src/app/components/Dashboard.tsx
  - src/app/components/Navigation.tsx
  - src/app/components/MobileBottomNav.tsx
  - src/app/routes/index.tsx
autonomous: true
requirements:
  - GOAL-01
  - GOAL-02
  - GOAL-03
  - GOAL-04
  - GOAL-05
  - GOAL-06
  - GOAL-07
  - GOAL-08

must_haves:
  truths:
    - "PHOENIX/ELITE user can create a frequency goal (workouts per week) and see it on the Goals page"
    - "PHOENIX/ELITE user can create a volume goal (total weight per week) and see progress"
    - "PHOENIX/ELITE user can create a PR goal (target weight for specific exercise by deadline)"
    - "Each active goal displays a circular progress ring showing percent completion"
    - "Achieving a goal triggers a celebration animation"
    - "FREE tier is limited to 1 active goal; PHOENIX/ELITE get 3 active goals"
    - "User can edit or archive goals without losing progress history"
    - "Dashboard shows a goal progress summary widget in the right column"
  artifacts:
    - path: "supabase/migrations/20260217_phase11_goals.sql"
      provides: "user_goals table with RLS and goal limit enforcement"
      contains: "CREATE TABLE IF NOT EXISTS user_goals"
    - path: "src/schemas/goals.ts"
      provides: "Zod schema for goal validation and transforms"
      contains: "goalSchema"
    - path: "src/queries/goals.ts"
      provides: "TanStack Query options for goal fetching"
      contains: "goalsOptions"
    - path: "src/mutations/goals.ts"
      provides: "Goal CRUD mutations (create, update, archive)"
      exports: ["useCreateGoal", "useUpdateGoal", "useArchiveGoal"]
    - path: "src/app/components/Goals.tsx"
      provides: "Goals page with creation form, list, and progress rings"
    - path: "src/app/components/GoalProgressRing.tsx"
      provides: "SVG circular progress ring component"
    - path: "src/app/components/GoalCelebration.tsx"
      provides: "Goal achievement celebration animation"
    - path: "src/app/components/GoalDashboardWidget.tsx"
      provides: "Dashboard summary card for active goals"
  key_links:
    - from: "src/app/components/Goals.tsx"
      to: "src/queries/goals.ts"
      via: "useQuery with goalsOptions"
      pattern: "goalsOptions"
    - from: "src/app/components/Goals.tsx"
      to: "src/mutations/goals.ts"
      via: "useCreateGoal mutation"
      pattern: "useCreateGoal"
    - from: "src/app/components/GoalDashboardWidget.tsx"
      to: "src/queries/goals.ts"
      via: "useQuery for active goals"
      pattern: "goalsOptions"
    - from: "src/app/components/Goals.tsx"
      to: "src/app/components/GoalCelebration.tsx"
      via: "isOpen state when goal.status changes to completed"
      pattern: "GoalCelebration"
---

<objective>
Build the complete goal setting and tracking feature: database table, data layer (schema + queries + mutations), Goals page with creation form and progress rings, celebration animation on goal achievement, and Dashboard widget.

Purpose: Goal tracking is a core engagement feature — users set targets and see visual progress, driving return visits and subscription value for PHOENIX/ELITE tiers.
Output: Working /goals page with 3 goal types, progress visualization, celebrations, tier gating, and Dashboard widget.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-new-features/11-RESEARCH.md

# Key existing patterns to follow:
@src/queries/keys.ts
@src/mutations/community.ts
@src/schemas/transforms.ts
@src/hooks/useSubscription.ts
@src/app/components/celebrations/PRCelebration.tsx
@src/app/components/Dashboard.tsx
@src/app/routes/index.tsx
@src/app/components/Navigation.tsx
@supabase/migrations/20260217_phase10_tables.sql
</context>

<tasks>

<task type="auto">
  <name>Task 1: Database migration + data layer (schema, queries, mutations, keys)</name>
  <files>
    supabase/migrations/20260217_phase11_goals.sql
    src/lib/database.types.ts
    src/schemas/goals.ts
    src/queries/keys.ts
    src/queries/goals.ts
    src/mutations/goals.ts
  </files>
  <action>
**Migration (supabase/migrations/20260217_phase11_goals.sql):**
Create `user_goals` table:
- `id UUID PRIMARY KEY DEFAULT gen_random_uuid()`
- `user_id UUID NOT NULL REFERENCES auth.users(id) ON DELETE CASCADE`
- `goal_type TEXT NOT NULL CHECK (goal_type IN ('frequency', 'volume', 'pr'))` — three types per user decision
- `target_value NUMERIC NOT NULL` — workouts/week, kg/week, or target weight
- `target_unit TEXT NOT NULL` — 'workouts/week', 'kg/week', 'kg'
- `exercise_name TEXT` — only for PR goals (nullable)
- `deadline TIMESTAMPTZ` — only for PR goals (nullable)
- `period TEXT NOT NULL DEFAULT 'weekly' CHECK (period IN ('weekly', 'monthly'))` — for frequency/volume goals
- `status TEXT NOT NULL DEFAULT 'active' CHECK (status IN ('active', 'completed', 'archived'))`
- `completed_at TIMESTAMPTZ`
- `created_at TIMESTAMPTZ NOT NULL DEFAULT now()`
- `updated_at TIMESTAMPTZ NOT NULL DEFAULT now()`

Enable RLS immediately after CREATE TABLE. Policies:
- SELECT: `user_id = auth.uid()` (users see own goals)
- INSERT: `user_id = auth.uid() AND public.user_subscription_tier() IN ('PHOENIX', 'ELITE')`
- UPDATE: `user_id = auth.uid()`
- DELETE: `user_id = auth.uid()`

Add goal limit enforcement trigger:
```sql
CREATE OR REPLACE FUNCTION public.check_goal_limit()
RETURNS TRIGGER LANGUAGE plpgsql SECURITY DEFINER AS $$
DECLARE
  active_count INT;
  max_goals INT;
  tier TEXT;
BEGIN
  tier := public.user_subscription_tier();
  IF tier = 'FREE' THEN max_goals := 1;
  ELSE max_goals := 3;
  END IF;
  SELECT COUNT(*) INTO active_count FROM user_goals
    WHERE user_id = NEW.user_id AND status = 'active';
  IF active_count >= max_goals THEN
    RAISE EXCEPTION 'Goal limit reached for your subscription tier'
      USING ERRCODE = 'P0001';
  END IF;
  RETURN NEW;
END; $$;
CREATE TRIGGER enforce_goal_limit BEFORE INSERT ON user_goals
  FOR EACH ROW EXECUTE FUNCTION public.check_goal_limit();
```

Indexes: `CREATE INDEX idx_goals_user_status ON user_goals(user_id, status);`

**database.types.ts:** Add `user_goals` type stub matching the migration schema.

**Schema (src/schemas/goals.ts):**
Zod schema with transforms: parse `created_at`/`updated_at`/`completed_at`/`deadline` strings to Date objects. Export `goalSchema`, `createGoalSchema` (for form validation: `goal_type`, `target_value`, `exercise_name`, `deadline`, `period`), and inferred types.

**Query keys (src/queries/keys.ts):**
Add `goals` block:
```typescript
goals: {
  all: ["goals"] as const,
  byUser: (userId: string) => [...queryKeys.goals.all, userId] as const,
  progress: (userId: string) => [...queryKeys.goals.all, "progress", userId] as const,
},
```

**Queries (src/queries/goals.ts):**
- `goalsOptions(userId: string)` — fetches active + completed goals, validates with Zod
- `goalProgressOptions(userId: string)` — fetches workout sessions data needed for progress computation. For frequency goals: count distinct workout days in current period. For volume goals: sum total_volume in current period. For PR goals: check personal_records for the target exercise. **IMPORTANT:** Use Zod-transformed data to avoid weight doubling pitfall.

**Mutations (src/mutations/goals.ts):**
- `useCreateGoal()` — insert with toast.success("Goal created"), invalidates goals.all
- `useUpdateGoal()` — update goal fields, invalidates goals.all
- `useArchiveGoal()` — sets status='archived', invalidates goals.all (preserves historical data per GOAL-07)
All follow the confirmed mutation pattern (not optimistic) matching `useVote` in community.ts.
  </action>
  <verify>
- `npm run build` compiles without TypeScript errors
- Migration SQL is syntactically valid (no runtime check needed — verified on deploy)
- Schema exports are importable: `import { goalSchema } from "@/schemas/goals"`
  </verify>
  <done>
- user_goals migration exists with RLS + goal limit trigger
- Zod schema validates and transforms goal data
- Query keys, query options, and 3 mutation hooks exist
- All follow established codebase patterns (queryOptions, confirmed mutations, Zod validation)
  </done>
</task>

<task type="auto">
  <name>Task 2: Goals page, progress rings, celebrations, Dashboard widget, navigation</name>
  <files>
    src/app/components/Goals.tsx
    src/app/components/GoalProgressRing.tsx
    src/app/components/GoalCelebration.tsx
    src/app/components/GoalDashboardWidget.tsx
    src/app/components/Dashboard.tsx
    src/app/components/Navigation.tsx
    src/app/components/MobileBottomNav.tsx
    src/app/routes/index.tsx
  </files>
  <action>
**GoalProgressRing (src/app/components/GoalProgressRing.tsx):**
Pure SVG circular progress ring (~50 lines) per research Pattern 4:
- Props: `progress` (0-100), `size` (default 80), `strokeWidth` (default 6), `color` (default `var(--phoenix-ember)`)
- Background track circle + animated progress arc with `stroke-dasharray`/`stroke-dashoffset`
- `transition-all duration-700 ease-out` for smooth animation
- Centered percentage text inside the ring

**GoalCelebration (src/app/components/GoalCelebration.tsx):**
Follow PRCelebration pattern exactly: `isOpen` + `onClose` props, phased animation with timeouts, AnimatePresence overlay + particle burst + card reveal. Card shows:
- Goal type icon (Target for frequency, TrendingUp for volume, Award for PR)
- "GOAL ACHIEVED!" title with gradient text
- Goal description and achieved value
- "Tap anywhere to continue"

**Goals page (src/app/components/Goals.tsx):**
Full-page component matching existing page patterns (motion.div entrance animations, Card components, dark theme):
1. Header: "Training Goals" title + "New Goal" button (disabled with tooltip if at limit)
2. Tier gate: `useSubscription().isPremium` — FREE users see upgrade prompt (use EmptyState component from ui/empty-state if it exists, else simple Card)
3. Goal creation: Dialog with form fields:
   - Goal type selector (Tabs: Frequency | Volume | PR)
   - Frequency: target workouts per week (number input)
   - Volume: target kg per week (number input)
   - PR: exercise name (text input from ExercisePicker or plain input), target weight, optional deadline
   - Submit calls `useCreateGoal()`
4. Active goals list: Card per goal with:
   - GoalProgressRing showing computed progress
   - Goal description (type-specific label)
   - Progress text ("3/5 workouts this week")
   - Edit button → opens edit Dialog (same form, pre-populated)
   - Archive button → calls useArchiveGoal()
5. Completed goals section (collapsible): shows achieved goals with completion date
6. Goal achievement detection: Compare progress to target on each render. When progress >= 100% and goal status is still 'active', trigger mutation to set status='completed' and show GoalCelebration.

**Progress computation logic (inside Goals.tsx or extracted helper):**
- Frequency: count distinct workout days in current week from workoutListOptions data
- Volume: sum total_volume from workoutListOptions data for current week/month
- PR: check if any personal_records entry for the exercise exceeds target_value
Use existing query data (already Zod-transformed — avoids weight doubling).

**GoalDashboardWidget (src/app/components/GoalDashboardWidget.tsx):**
Compact card for Dashboard right column:
- Shows up to 3 active goals with mini progress rings (size=48)
- "View All" link to /goals
- Tier-gated: only renders for premium users
- If no active goals: "Set your first goal" CTA link

**Dashboard.tsx integration:**
Import GoalDashboardWidget. Add it to the right column (`<div className="space-y-6">` section) after existing Quick Stats card. Wrap in motion.div with entrance animation matching existing pattern.

**Navigation.tsx:**
Add `{ path: "/goals", label: "Goals", icon: Target }` to navItems array. Import `Target` from lucide-react. Place after "/challenges" entry.

**MobileBottomNav.tsx:**
Add Goals to the "More" drawer menu items if it uses a similar pattern. Check the component and add accordingly.

**Routes (src/app/routes/index.tsx):**
- Add lazy import for Goals: `const Goals = lazy(() => import("@/app/components/Goals").then(m => ({ default: m.Goals })))`
- Add route: `<Route path="/goals" element={<Goals />} />` inside the AppLayout protected routes
  </action>
  <verify>
- `npm run build` compiles without errors
- `npm run test` passes (existing tests don't break)
- Goals page is accessible at /goals route
- Navigation shows Goals item
- Dashboard renders GoalDashboardWidget in right column
  </verify>
  <done>
- Goals page renders with creation form, progress rings, and celebration animations
- SVG progress ring animates smoothly from 0 to computed progress
- Goal creation respects tier limits (1 for FREE, 3 for PHOENIX/ELITE)
- Edit and archive work without data loss
- Dashboard widget shows active goal summaries
- /goals route, Navigation entry, and MobileBottomNav entry all wired
  </done>
</task>

</tasks>

<verification>
1. `npm run build` — zero TypeScript errors
2. `npm run test` — all existing tests pass
3. Manual: Navigate to /goals, create a frequency goal, verify progress ring renders
4. Manual: Check Dashboard right column shows goal widget
5. Manual: Verify FREE tier shows 1-goal limit, PHOENIX shows 3-goal limit
</verification>

<success_criteria>
- A PHOENIX user can create all 3 goal types and see progress rings on the Goals page
- Goal achievement triggers a celebration animation
- FREE tier is limited to 1 active goal
- Dashboard displays goal summary widget
- Goals appear in navigation (desktop and mobile)
</success_criteria>

<output>
After completion, create `.planning/phases/11-new-features/11-01-SUMMARY.md`
</output>
