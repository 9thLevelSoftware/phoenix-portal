---
phase: 16-visual-depth-surfaces
plan: 02
type: execute
wave: 2
depends_on: [16-01]
files_modified:
  - src/app/components/LandingPage.tsx
  - src/app/components/Dashboard.tsx
  - src/app/components/PersonalRecords.tsx
  - src/app/components/Analytics.tsx
  - src/app/components/Challenges.tsx
  - src/app/components/Community.tsx
  - src/app/components/Goals.tsx
  - src/app/components/Recovery.tsx
  - src/app/components/RoutinesEnhanced.tsx
  - src/app/components/TrainingCycles.tsx
  - src/app/components/WorkoutHistory.tsx
  - src/app/components/SessionDetail.tsx
  - src/app/components/Profile.tsx
  - src/app/components/ComparisonView.tsx
  - src/app/components/AppSidebar.tsx
  - src/app/components/Navigation.tsx
  - src/app/components/PrivacyPolicy.tsx
  - src/app/components/ResetPassword.tsx
  - src/app/components/CelebrationDemo.tsx
  - src/app/components/GoalCelebration.tsx
  - src/app/components/celebrations/PRCelebration.tsx
  - src/app/components/celebrations/StreakMilestone.tsx
  - src/app/components/celebrations/ChallengeWon.tsx
autonomous: true
requirements: [BUG-07]

must_haves:
  truths:
    - "Gradient text (bg-clip-text text-transparent) appears ONLY on hero h1 headlines: LandingPage hero h1 and Dashboard welcome h1 username span"
    - "All section headers (h2 elements) across the entire app use solid text-white, not gradient text"
    - "All logo/branding text spans (AppSidebar, Navigation) use solid text-primary, not gradient text"
    - "All celebration modal headlines use solid text-white, not gradient text"
    - "All stat numbers displayed with gradient text use solid text-primary instead"
  artifacts:
    - path: "src/app/components/LandingPage.tsx"
      provides: "Only 1 gradient text instance (hero h1)"
      contains: "bg-clip-text"
    - path: "src/app/components/Dashboard.tsx"
      provides: "Only 1 gradient text instance (welcome h1 username)"
      contains: "bg-clip-text"
  key_links:
    - from: "grep bg-clip-text src/"
      to: "exactly 2 files with instances"
      via: "codebase-wide sweep"
      pattern: "bg-clip-text text-transparent"
---

<objective>
Sweep all 47 gradient text instances across 23 files, replacing all but 2 hero h1 usages with solid text colors (text-white for headers, text-primary for branding/stats).

Purpose: Reserve gradient text for hero headlines only, creating a clear visual hierarchy where gradient treatment signals "most important headline" rather than being diluted across every heading.
Output: Only LandingPage hero h1 and Dashboard welcome h1 retain gradient text. All other instances use solid colors.
</objective>

<execution_context>
@C:/Users/dasbl/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/dasbl/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-visual-depth-surfaces/16-RESEARCH.md
@.planning/phases/16-visual-depth-surfaces/16-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep gradient text from high-instance files (LandingPage, Dashboard, PersonalRecords, Analytics, Challenges, Community, Goals, Recovery)</name>
  <files>src/app/components/LandingPage.tsx, src/app/components/Dashboard.tsx, src/app/components/PersonalRecords.tsx, src/app/components/Analytics.tsx, src/app/components/Challenges.tsx, src/app/components/Community.tsx, src/app/components/Goals.tsx, src/app/components/Recovery.tsx</files>
  <action>
**FIRST:** Run `grep -rn "bg-clip-text text-transparent" src/ --include="*.tsx"` to get the current full list with line numbers. Use this as the authoritative checklist.

**Replacement rules (apply to ALL instances in ALL files):**

| Context | Old Pattern | New Pattern |
|---------|-------------|-------------|
| Section h2 headers | `<span className="bg-gradient-to-r from-X to-Y bg-clip-text text-transparent">Text</span>` | Remove the `<span>` wrapper entirely. Put the text directly in the `<h2>`. Add `text-white` to the `<h2>` className. |
| Card titles / h3 headers | `<span className="...bg-clip-text text-transparent">Text</span>` | Remove span wrapper, add `text-white` to parent element |
| Stat numbers (large numeric displays) | `<span className="...bg-clip-text text-transparent">{number}</span>` | Replace with `<span className="text-primary">{number}</span>` (keep span for the number, just change classes) |
| Brand/logo text | `<span className="...bg-clip-text text-transparent">Project Phoenix</span>` | `<span className="text-primary">Project Phoenix</span>` |

**Per-file instructions:**

**LandingPage.tsx** (8 instances, KEEP 1, fix 7):
- KEEP: The hero h1 "Project Phoenix" gradient text — this is THE hero headline
- FIX (7): All `<h2>` section headers ("Elevate Your Training", "Choose Your Path", "Frequently Asked Questions", etc.), the "Phoenix" brand span in the footer, any pricing section gradient headers, any other gradient spans that are not the hero h1

**Dashboard.tsx** (4 instances, KEEP 1, fix 3):
- KEEP: The welcome h1 username span (e.g., `<span className="bg-gradient-to-r...">Username</span>` inside the welcome greeting h1)
- FIX (3): Desktop streak number → `text-primary`; any section h2 headers → `text-white`; any other gradient spans

**PersonalRecords.tsx** (4 instances, fix ALL):
- FIX: Section headers → `text-white`; stat numbers (PR values) → `text-primary`

**Analytics.tsx** (2 instances, fix ALL):
- FIX: Section headers → `text-white`

**Challenges.tsx** (2 instances, fix ALL):
- FIX: Section headers → `text-white`

**Community.tsx** (2 instances, fix ALL):
- FIX: Section headers → `text-white`

**Goals.tsx** (2 instances, fix ALL):
- FIX: Section headers → `text-white`; any stat numbers → `text-primary`

**Recovery.tsx** (2 instances, fix ALL):
- FIX: Section headers → `text-white`; any stat numbers → `text-primary`

**When removing span wrappers:** If the gradient span is the ONLY child of its parent (e.g., `<h2><span class="gradient...">Text</span></h2>`), remove the span and put text + `text-white` class directly on the h2. If the span is alongside other content, just replace the gradient classes with `text-white` or `text-primary` on the span.

**CRITICAL:** Do NOT touch LandingPage hero h1 or Dashboard welcome h1 username span — those are the only 2 KEEP instances in the entire codebase.
  </action>
  <verify>
Run `grep -c "bg-clip-text text-transparent" src/app/components/LandingPage.tsx` — should return exactly 1.
Run `grep -c "bg-clip-text text-transparent" src/app/components/Dashboard.tsx` — should return exactly 1.
Run `grep -c "bg-clip-text text-transparent" src/app/components/PersonalRecords.tsx` — should return 0.
Run `grep -c "bg-clip-text text-transparent" src/app/components/Analytics.tsx` — should return 0.
Run `npm run build` — zero errors.
  </verify>
  <done>LandingPage has exactly 1 gradient text instance (hero h1). Dashboard has exactly 1 (welcome h1). PersonalRecords, Analytics, Challenges, Community, Goals, Recovery have 0.</done>
</task>

<task type="auto">
  <name>Task 2: Sweep gradient text from remaining 15 files (sidebars, celebrations, detail pages, utilities)</name>
  <files>src/app/components/RoutinesEnhanced.tsx, src/app/components/TrainingCycles.tsx, src/app/components/WorkoutHistory.tsx, src/app/components/SessionDetail.tsx, src/app/components/Profile.tsx, src/app/components/ComparisonView.tsx, src/app/components/AppSidebar.tsx, src/app/components/Navigation.tsx, src/app/components/PrivacyPolicy.tsx, src/app/components/ResetPassword.tsx, src/app/components/CelebrationDemo.tsx, src/app/components/GoalCelebration.tsx, src/app/components/celebrations/PRCelebration.tsx, src/app/components/celebrations/StreakMilestone.tsx, src/app/components/celebrations/ChallengeWon.tsx</files>
  <action>
Continue the sweep from Task 1 using the same replacement rules. Apply to all remaining files:

**AppSidebar.tsx** (1 instance):
- FIX: Logo text "Project Phoenix" → `text-primary` (keep `font-semibold` and other classes, just replace gradient classes)

**Navigation.tsx** (1 instance):
- FIX: Logo text → `text-primary`

**RoutinesEnhanced.tsx** (2 instances):
- FIX: Section headers → `text-white`; any stat numbers → `text-primary`

**TrainingCycles.tsx** (3 instances):
- FIX: All section headers → `text-white`; stat numbers → `text-primary`

**WorkoutHistory.tsx** (2 instances):
- FIX: Section headers → `text-white`

**SessionDetail.tsx** (2 instances):
- FIX: Section headers → `text-white`; stat numbers → `text-primary`

**Profile.tsx** (1 instance):
- FIX: Stat number → `text-primary`

**ComparisonView.tsx** (1 instance):
- FIX: Section header → `text-white`

**PrivacyPolicy.tsx** (2 instances):
- FIX: Page title / section headers → `text-white`

**ResetPassword.tsx** (1 instance):
- FIX: Page title → `text-white`

**CelebrationDemo.tsx** (1 instance):
- FIX: Demo title → `text-white`

**GoalCelebration.tsx** (1 instance):
- FIX: Celebration headline → `text-white`

**celebrations/PRCelebration.tsx** (1 instance):
- FIX: Celebration headline → `text-white`

**celebrations/StreakMilestone.tsx** (1 instance):
- FIX: Celebration headline → `text-white`

**celebrations/ChallengeWon.tsx** (1 instance):
- FIX: Celebration headline → `text-white`

**After all fixes:** Run a final codebase-wide grep to confirm exactly 2 instances remain:
```bash
grep -rn "bg-clip-text text-transparent" src/ --include="*.tsx"
```
Expected: exactly 2 lines — one in LandingPage.tsx (hero h1), one in Dashboard.tsx (welcome h1 username).
  </action>
  <verify>
Run `grep -rn "bg-clip-text text-transparent" src/ --include="*.tsx" | wc -l` — must return exactly 2.
Run `grep -rn "bg-clip-text text-transparent" src/ --include="*.tsx"` — both results must be hero h1 lines.
Run `npm run build` — zero errors.
  </verify>
  <done>Entire codebase has exactly 2 gradient text instances: LandingPage hero h1 and Dashboard welcome h1. All other 45 instances replaced with solid text-white or text-primary. Build passes.</done>
</task>

</tasks>

<verification>
1. `npm run build` passes with zero errors
2. `grep -rn "bg-clip-text text-transparent" src/ --include="*.tsx" | wc -l` returns exactly 2
3. The 2 remaining instances are: LandingPage hero h1 ("Project Phoenix") and Dashboard welcome h1 (username span)
4. No section h2 header anywhere uses gradient text
5. AppSidebar and Navigation logo text use `text-primary` not gradient
6. All celebration modals use `text-white` headlines
</verification>

<success_criteria>
- Gradient text is visually reserved for hero headlines only — scanning any page other than the landing hero or dashboard welcome shows zero gradient-on-text effects
- Section headers across all pages use consistent solid white text
- Brand text in sidebar and navigation uses solid ember/primary color
- Build passes with zero errors after sweeping all 23 files
</success_criteria>

<output>
After completion, create `.planning/phases/16-visual-depth-surfaces/16-02-SUMMARY.md`
</output>
