# Required: some dev/test peers (React 19, Vite 7, Testing Library) have not
# all published matching peer ranges yet, so npm's strict peer resolution
# fails a clean install. Revisit periodically and drop this once upstream
# peer ranges cover the chosen stack.
legacy-peer-deps=true
