# Historical Archive

Files under `docs/archive/` are preserved historical context. They are not
active project guidance unless a canonical doc outside this archive links to a
specific archived decision and explicitly says it is still current.

Use `docs/index.md` and the top-level docs as the source of truth.
