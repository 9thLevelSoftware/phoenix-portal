# Guidelines

This directory is retained for tools that look for a `guidelines/` entrypoint.

Canonical guidance lives in:

- `../docs/FRONTEND.md`
- `../docs/ENGINEERING_STANDARDS.md`
- `../docs/QUALITY.md`

Do not add standalone rules here. Add durable project guidance to `docs/` so all
agents and CI checks read the same source of truth.
